---
title: "E7 build — Squadron becomes the unit of user choice in every picker"
kind: ticket
status: 1
---

# E7 — Squadron picker conversion

**Governing artifacts:** `../../product/features/squadron.md` §E7 (definitional law, ruled 2026-08-31), `../picker-and-self-messaging-rulings-2026-08-31.md`, DV1/DV3 (`../../product/dogfood-v0.md`). Origin: Jackson's #20 acceptance pass — the new-thread flow still presented the upstream PROJECT picker before the prompt.

## Goal

Every context-picking surface offers **Squadrons**; "project" stops being a user-facing noun (it remains DV1 substrate — reference, never identity).

## Scope

- **Convert the new-thread flow's picker** to Squadron choice.
- **Convert the command palette** (ruled in for v0).
- **Inventory sweep**: enumerate every remaining project-picking surface in the app; convert what's in reach, board what isn't with a named disposition — no silent survivors.

## Build-critical guard (Jackson's ruling, verbatim intent — build-blocking)

Pickers key off **Squadrons (Registrar truth)**, never derived from projects — **two Squadrons over one folder must render as two distinct choices**, or DV1's acceptance criterion is silently broken in the UI. Folders render as attributes inside the flow (SC1 palette / DV3 disposition), never as a peer choice.

## Out of scope

Multi-folder (returns with its milestone). Roster/Fleet-page surfaces (own lane). Any change to the DV1 substrate model.

## Dependencies

#20 merged (Squadron creation + scope machinery). Staffs as a NEW group under the new system: agent-ops registration, `~/.pr-group` playbooks on the `personal` branch, design gate satisfied by E7's doc. Seam maps to the Director before upstream touches, as always.

## Acceptance

In the dev app: new-thread flow and command palette both offer Squadrons (never projects) as the choice; two Squadrons sharing a folder appear as two distinct entries whose selection scopes correctly (Registrar-home assertion); folder information appears only as attribute display within a Squadron context; the inventory sweep's list is recorded with per-surface disposition; no user-facing surface renders "project" as a noun. Screenshots per the evidence process. Baseline green.

## Inventory sweep — E7 build record

**Method (re-run before review):**

```sh
rg -l --glob '*.{ts,tsx}' 'useProjects|projectId|selectProject|onSelectProject' apps/web/src apps/mobile/src
rg -l --glob '*.{ts,tsx}' -i 'project' apps/web/src apps/mobile/src
rg -n --glob '*.{ts,tsx}' -i '(aria-label|title|alt|placeholder)=[^\n]*project' apps/web/src apps/mobile/src
rg -n --glob '*.{ts,tsx}' 'handleNewThread\(|openCommandPalette\(' apps/web/src apps/mobile/src
```

The first two searches deliberately do not share a failure mode: symbols catch reworded choice
paths, while copy catches presentation that has lost the obvious symbol. The third is a mandatory
accessibility-property pass; screenshots cannot prove those strings. The final trace identifies
new-thread entry points and their palette routing. Every candidate below is either converted or
has a named return; this is an inventory, not a claim that every project-shaped substrate becomes
a Squadron in E7.

| Surface                                                                                   | Evidence                                                                                                                                                                                                                                                                                                                               | E7 disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Web command palette context search and `New thread in…` submenu                           | `components/CommandPalette.tsx`, `CommandPalette.logic.ts`, `CommandPaletteResults.tsx:98`                                                                                                                                                                                                                                             | **Converted.** Registrar directory yields Squadron-keyed rows that render the Squadron name only; folder data remains search/availability/launch substrate, never row identity or a second line. Existing navigation matches immutable Registrar home only; otherwise the existing draft launch is immediately given the selected Squadron ID through SQ1's carrier. The empty-state copy also says “Squadrons,” and its derived Squadron menus recompute when Registrar entries resolve.                                                                                                                                                                                                                           |
| Web sidebar New thread                                                                    | `components/Sidebar.tsx`, `Sidebar.logic.ts`                                                                                                                                                                                                                                                                                           | **Converted.** It opens the Squadron picker unless the Registrar directory is ready and has exactly one Squadron; project/folder count and shift-click are no longer a choice-bypassing proxy. Per the #193 ruling, loading renders only a loading state, while ready-with-zero alone exposes the existing Create Squadron dialog; the toolbar takes that same ready-zero path rather than opening an empty picker.                                                                                                                                                                                                                                                                                                 |
| Web keyboard new-thread shortcuts                                                         | `routes/_chat.tsx`                                                                                                                                                                                                                                                                                                                     | **Converted.** `chat.new` and `chat.newLocal` use the same exact-one Registrar choice and draft carrier as the sidebar; otherwise they open the Squadron picker. A sole Squadron with no available folder fails closed with a named error instead of swallowing the keypress.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Web Sidebar branch-thread action                                                          | `components/Sidebar.tsx:3040-3085`, `SquadronPicker.logic.ts`                                                                                                                                                                                                                                                                          | **Converted.** The in-reach `new-thread-on-branch` door resolves the source thread's immutable Registrar home, then launches through the shared Squadron draft carrier with the home Squadron's folder; it never treats the source folder as identity. A missing known-home entry, unavailable read, or multiple candidates opens the existing Squadron picker; a missing chosen folder fails closed with a named error. The orphaned `useThreadActionMenu` implementation (including its stranded E7 #153 edits) is deliberately deleted rather than retained as a second door.                                                                                                                                    |
| Web first-run, current-thread, and draft composer                                         | `_chat.index.tsx`, `ChatView.tsx`, `ChatView.logic.ts`, `chat/ChatHeader.tsx`, `DraftHeroHeadline.tsx`, `useHandleNewThread.ts`                                                                                                                                                                                                        | **Converted.** The current-thread action uses its known immutable Registrar home, otherwise the ready/exact-one Squadron choice, and always uses SQ1's carrier. Its single shared `squadronDraftScopeKey` stores under `scopedThreadKey(scopeThreadRef(folder.environmentId, returnedThreadId))`, never the local `draftId`. It never uses the active folder as identity. The index route auto-starts only for an ambient selected or exact-one Squadron; multi renders an explicit choice state and zero remains the existing first-run creation control, never a project-derived launch or automatic picker. A missing selected folder is a permanent honest state; only a transient launch failure offers retry. |
| Web Squadron creation folder control                                                      | `j5/squadron/SquadronCreateForm.tsx:72,84`                                                                                                                                                                                                                                                                                             | **Converted copy-only per Spawner ruling.** “Project picker” becomes “folder”; the control's accessibility label now matches its visible Choose folder / Change folder text. Bus payload, state types, and add-project mechanics stay SQ1-owned and unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Web scheduled-task selector                                                               | `settings/ScheduledTasksSettings.tsx`                                                                                                                                                                                                                                                                                                  | **Boarded — recommended follow-up: scheduling-context milestone.** DV5 intentionally refuses scheduled new-thread creation until a truthful scheduling-context carrier exists; converting this selector without that contract would invent Squadron provenance.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Web pull-request filter and assignment                                                    | `pullRequest/PullRequestListFilters.tsx`, `_chat.pull-requests.tsx`, `pullRequestProjectAssignment.logic.ts`                                                                                                                                                                                                                           | **Boarded — recommended follow-up: pull-request Squadron query contract.** These select repository/workspace data for filtering or assignment, not an agent's working home; a Squadron filter needs a separately designed query contract.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Web legacy sidebar                                                                        | `components/LegacySidebar.tsx`, reachable through `settings/SettingsPanels.tsx:1786`                                                                                                                                                                                                                                                   | **Boarded — recommended follow-up: legacy sidebar conversion.** It remains user-reachable and has per-folder new-thread actions, so it is not a dead-path exemption.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Web settings and active-context file/content surfaces                                     | `settings/ProjectSettingsPanel.tsx`, `SettingsPanels.tsx`, `search/ProjectContentSearchDialog.tsx`, file pickers                                                                                                                                                                                                                       | **Boarded — recommended follow-up: substrate-noun cleanup.** They configure or operate on the current folder substrate rather than pick an agent home. “Project” noun removal here is a broader copy/IA lane, not a silent E7 omission.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Mobile new-task flow, share destination, home filters/navigation, add-folder and grouping | `features/threads/NewTaskRouteScreen.tsx`, `NewTaskDraftScreen.tsx`, `new-task-flow-provider.tsx`, `new-task-project-selection.ts`, `features/home/*`, `ThreadNavigationSidebar.tsx`, `features/projects/AddProjectScreen.tsx`, `features/settings/SettingsProjectGroupingRouteScreen.tsx`, `features/sharing/incoming-share-inbox.ts` | **Boarded — recommended follow-up: mobile-Squadron milestone.** DV5's named mobile-new/offline-drain return is precedent, but not automatic coverage: this record explicitly carries all mobile context paths forward until mobile has a truthful Squadron directory/home carrier.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Other web/mobile user-facing project nouns                                                | source-control, scripts, connection, file-browser, git-control, settings, outbox, and thread-list copy found by the copy/accessibility sweep                                                                                                                                                                                           | **Boarded — recommended follow-up: substrate-noun cleanup.** E7 converts working-context choice now; broader wording cannot be safely renamed without per-surface product ownership.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

The boarded rows require named follow-up work before their disposition can close. No new ticket is
created by this record; J5 PR-group policy requires Jackson's explicit approval to create one.
Board disposition is decision #77 and ask #33; deferred findings #146–#151 await Jackson.
The post-review CodeRabbit dispositions (#189–#197, including #193's ready-only empty/Create
state) are implemented in the converted rows above; no additional context or creation surface is
introduced.

**Recovery record — #201:** `useThreadActionMenu` was stranded by the v2 rewrite at
`b2ee17d7c`; it had no production callers in the final route trace. Its E7 #153 carrier edit is
therefore deleted with the orphan rather than resurrected. The still-live
`threadActionMenu.logic.test.ts` remains: it proves the menu item rendered and handled by
`Sidebar.tsx`, not the deleted hook.

**Recovery record — ambient launch accommodation:** pre-dogfood state contains no legacy
unselected drafts to recover. The E7 door trace proves every successful converted first-send door
writes its Squadron through SQ1's draft-local carrier before composition, except an existing
thread's immutable Registrar home is already its carrier; ambient scope remains a zoomed-out
presentation/filter choice only. A zero-message draft without either carrier now refuses to send
with “Choose a Squadron before sending” instead of copying and freezing ambient context.

**Recovery record — picker row shape:** the later dogfood ruling removes folder-description
second lines from Squadron picker rows. Their visible identity is the Squadron name alone; folder
data remains only for search, availability, and the explicit launch substrate.
