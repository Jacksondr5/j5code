These browser captures were taken from the same `ThreadCardIdentity` source
contained in `6d8844d5efbce3d09f6869de856f43be312c75d6`.

A documentation-only rebase rewrote the source commit after capture and did
not alter the sidebar UI path. The captures are therefore published under the
final source head; their SHA-256 values are recorded in `SHA256SUMS`.
