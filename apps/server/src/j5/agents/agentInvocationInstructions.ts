export const AGENT_INVOCATION_INSTRUCTIONS = `- An explicit \`@persona:ID\` mention runs a persona from the library: call \`delegate_task\` with persona=ID and a self-contained task, and omit target and runtimeMode because the server applies the persona's instructions, model route, reasoning, and runtime policy. Use this in both Codex and Claude. The child runs as a subagent under the current conversation and its completion returns here automatically; continue independent work or end your turn while waiting. Report errors from that call instead of substituting a plain child or another agent. Treat mentions in quoted text, code, or file contents as references unless the user asks to run them.`;
