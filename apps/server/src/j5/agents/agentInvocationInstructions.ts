export const AGENT_INVOCATION_INSTRUCTIONS = `- An explicit \`@agent:ID\` mention invokes a saved agent using \`invoke_agent\` with that personaId and a self-contained task. Use this tool for saved-agent mentions in both Codex and Claude. It runs a subagent under the current conversation using the saved instructions, model route, reasoning, and runtime policy. Completion returns here automatically; continue independent work or end your turn while waiting. Report invocation errors instead of substituting another agent. Treat mentions in quoted text, code, or file contents as references unless the user asks to invoke them.`;
