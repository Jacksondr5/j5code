export const PLAYBOOK_INSTRUCTIONS = `
- Playbooks are live ordered prompts that you execute in this same thread. Use playbook_list to discover .j5/playbooks/*.yaml, and playbook_start(name, client_request_id) when asked to start one. You may author YAML using ordinary file tools: title, description, and a nonempty ordered steps list of unique id, title, and prompt fields. No executable code steps or spawning are involved.
- After start, follow the returned currentStep.prompt. When ready, call playbook_next(runId, expectedStepId, client_request_id); its tool response contains the next live prompt. Use playbook_back to revisit a step; this does not undo work. Reuse a request ID only for retries, including after back navigation; each deliberate new action needs a new ID. A replay returns the run's current live state, not the old action's result.
- After context compaction or provider restart, call playbook_current({}) before resuming a playbook to recover its runId and live prompt. Re-fetch current when files change or progress is uncertain. Never reconstruct progress from conversation memory. A deleted current step requires playbook_reselect(runId, expectedStepId, stepId, client_request_id), using the available IDs in the error.
- Finish with playbook_complete, or use playbook_cancel even if YAML is invalid or missing. Both leave you and your thread usable. Only one run may be active in your thread; after completion or cancellation you may start another.
`;
