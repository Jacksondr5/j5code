# J5 Code fork discipline

J5 Code is a public fork of [pingdotgg/t3code](https://github.com/pingdotgg/t3code). The visible GitHub fork relationship preserves upstream attribution. This file records how we keep local work easy to audit and carry across upstream history changes.

## Remotes

Every working clone uses these remotes:

```text
origin   https://github.com/Jacksondr5/j5code.git
upstream https://github.com/pingdotgg/t3code.git
```

Reproduce that setup with:

```sh
git clone https://github.com/Jacksondr5/j5code.git
cd j5code
git remote add upstream https://github.com/pingdotgg/t3code.git
git fetch --all --prune
git switch j5/main
# j5/main is the canonical fork branch; do not use fork-local main; upstream refs live under the upstream remote.
```

## Add, don't modify

J5-specific code belongs in new files or new packages. Keep commits narrow and organized so they can be cherry-picked onto a new upstream base.

The only sanctioned edits to upstream-owned files are small appended integration cases, such as a new switch case or registry entry that makes a new J5-owned module reachable. Do not refactor surrounding upstream code while adding a case.

Treat these upstream areas as off-limits except for those explicit appended cases:

- `apps/server` core orchestration and persistence
- `packages/contracts`
- `packages/client-runtime`
- `apps/web` existing application components and render paths
- existing provider adapters and shared runtime modules
- vendored references under `.repos`

### Sanctioned appended integration cases

Against candidate upstream pin `67a2be0fdbee7afb64b691f147ed286a108c706b`, the inventory has 46 cases: 1–45 plus 15b. Existing numbers remain stable. Line coordinates and landing SHAs below describe historical integrations; use named symbols and tests after upstream movement. Branding has its own inventory in `BRANDING.md`; temporary defect patches are listed separately below.

1. A1's independent Squadron communication-ledger migration lane: `apps/server/src/persistence/Layers/Sqlite.ts` runs `runJ5CompatibleUpstreamMigrations` before `runJ5A2AMigrations`, inside upstream's `layerConfig` after `initializeV2Database` has copied `state.sqlite` to `statev2.sqlite`; both run only against `statev2.sqlite`. J5 adopts that copy-on-first-start unchanged (Jackson's 2026-09-24 decision #2): an existing `statev2.sqlite` wins and `state.sqlite` is never recopied or written again, so the cutover is one-way: writes after the first V2 boot are invisible to an older binary. Snapshot before the upgrade, and roll back across it by stopping writers and moving `statev2.sqlite` aside (`docs/j5/runbooks/dogfood-runtime.md`); never run old and new binaries against the two files. J5 tooling (`fleet-load.ts`, `devDeliverySeed.ts`, `dogfood-snapshot.sh`, `dogfood-local.sh`) targets `statev2.sqlite`; `apps/server/src/j5/persistence/StateV2Cutover.test.ts` covers the copy, no-recopy and failed-first-boot retry. Case 31 defines the compatibility wrapper; J5's own migration IDs remain in separate tracking tables and run after upstream migration completion.
2. The authenticated J5 MCP registration seam: `apps/server/src/mcp/McpHttpServer.ts:31-34` imports `J5McpIntegrationLive` and `J5OrchestratorSurfaceRegistrationLive`; `:246-252` selects the J5-owned orchestration allowlist and appends the J5 communication toolkit. The complete upstream `OrchestratorToolkitRegistrationLive` remains compiled at `:229-232` but is intentionally absent from the production merge. Registration stays in `apps/server/src/j5/a2a/mcp/registration.ts`; the combined HTTP/MCP runtime is provided once at case 5's server graph. `t3_thread_read` is intentionally re-declared in J5-owned code with factual thread-state prose because the upstream Tool description contains excluded app-owned/delegation semantics; never replace it with the upstream Tool constant during a rebase. J5 likewise re-declares `orchestrator_capabilities`, so upstream's pointer to `t3_thread_launch` in its description does not reach J5 agents. Open follow-up: upstream's `t3_worktree_handoff` Tool object is still exposed as-is, and its description now tells agents to use `t3_thread_launch` with `workspaceStrategy`, a tool J5 does not expose; re-declare it in J5-owned code like `t3_thread_read`. Upstream removed `t3_thread_start`; it stays in the exclusion assertions as a regression guard. `t3_thread_wait` is deliberately absent from the J5 surface since 2026-09-14 (a turn that blocks on another thread cannot receive the platform notices that queue behind it; see `orchestratorSurface.ts` and the registration tests that assert its absence); never re-add it. `list_participants` exposes every result field in snake_case at every depth, including nested agent `thread_id`, provenance `spawned_by_participant_id` / `source_participant_id`, `placement_parent_id`, and `display_name`; never restore any upstream camelCase directory or provenance field during a rebase. Later J5 verbs extend the fork-owned toolkit without another protected-file registration or runtime provider.
3. The internal delivery-dedup contract proof: `apps/server/src/orchestration-v2/runtimeLayer.test.ts:188-234`, test `replays an internal thread send without injecting a second message`.
4. The authenticated shared-toolkit integration proof: `apps/server/src/mcp/toolkits/worktree/registration.test.ts:15,70,83-90,122-146`, within test `production mcp layer lists worktree tools over http`; it asserts the retained J5 orchestration surface and the absence of excluded delegation, raw thread-send/interrupt, and thread-creation tools. The upstream provider-adapter read-only-tool assertion derives its expectation from the upstream toolkit rather than the J5 production surface, so it is intentionally unchanged; this direct J5 exact-registration proof is the gate of record. Revisit that protected upstream read-only assertion only after an actual drift incident that this J5 proof fails to catch.
5. Shared J5 server composition: `apps/server/src/server.ts` appends only `j5AuthenticatedRoutesLayer` after `websocketRpcRouteLayer`, provides `J5SquadronCreationLayer` once to the V2 runtime and `J5A2AAuxiliaryLayer` once around the combined HTTP/MCP graph. Both transport registrations consume that shared graph and must never create nested J5 runtimes. The aggregate in case 9 owns all J5 authenticated route additions, including the human inbox; upstream `http.ts` changes are limited to the separate branding boundary.
6. A4's visible human-inbox navigation append, relocated by B1: `apps/web/src/components/sidebar/SidebarChrome.tsx`, where `SidebarUtilityMenu` recognizes `/inbox` (and, since 2026-09-09, `/fleet`) for Back behavior and `SidebarChromeHeader` mounts the J5-owned `HumanInboxBell` and `FleetRailEntry` in a trailing control group, after the brand and optional environment pill. These current symbols supersede the original footer/grid anchors. The route files `routes/inbox.tsx` and `routes/fleet.tsx`, the pages, the bell, the rail entry, and their clients remain under J5-owned files (`j5/a2a`, `j5/fleet`); Settings, Pull Requests, Usage, and Back behavior are unchanged.
7. B3's thread A2A rendering seam: `apps/web/src/components/chat/MessagesTimeline.tsx:47` imports J5-owned `renderThreadA2ADelivery`; `TimelineRowContent` at `:1038-1046,:1073-1076` calls it only for `row.kind === "message" && row.message.role === "user"`, passes the complete `ChatMessage` plus one `formatDayAwareTimestamp` label from `TimelineRowCtx`, and renders the generic `UserTimelineRow` only when `a2aDelivery === null`. `UserTimelineRow` resolves context with upstream `resolveUserMessageContext`, except spawn briefs, which use J5 `resolvedSpawnBriefContext` (no records, no legacy upgrade). Classification, envelope parsing, raw fallback, and focused proof remain in `apps/web/src/j5/a2a/ThreadA2ARenderer.tsx` and `.test.tsx`; TA4 stays absent.
8. A2's provider steering seam: `apps/server/src/provider/T3OrchestrationInstructions.ts:7-12` replaces upstream delegated-task steering with current-head truth: provider-native Subagents remain provider-owned, platform `spawn_agent` creates a Peer Agent, and the spawn brief itself states the task plus whether and what reply is expected. A follow-up `send_message(..., expect_reply=true, intent="...")` opens an Exchange only for later work owed by an existing participant; it is never the second half of a spawn sequence. The schedule paragraph stays unchanged. Upstream's standalone-executable ACP fallback (`${T3_ACP_MCP_ENTRYPOINT:+…}`, entrypoint optional) is ported into J5's text; upstream's `t3_thread_launch`/`create_threads`/workspace-strategy guidance is deliberately not. `apps/server/src/provider/T3OrchestrationInstructions.test.ts`, test `steers to provider-native Subagents and platform Peer Agents`, is the narrow terminology/tool-availability proof and guards against restoring spawn-then-ask prose. The composed-prompt assertions in `apps/server/src/orchestration-v2/Adapters/CodexAdapterV2.test.ts` and `ClaudeAdapterV2.test.ts` import and assert `T3_CODE_ORCHESTRATION_INSTRUCTIONS` rather than pinning product wording; future authorized wording changes therefore require neither adapter-test edits nor another seam request. A rebase restoring an upstream literal is a protected-seam regression, not an authorized wording update. The ratified SP4 brief sentence lives exactly once in the actual J5 `spawn_agent` Tool description; global steering restates the contract without duplicating that sentence. Since 2026-09-17 (Jackson's proposal on PR #148) the steering names three shapes of help, Subagent, Peer Agent, and Crew, so an agent asked for a crew without the `/crew` prefix reaches `propose_crew` instead of the nearest thing it was told about; the same test pins the Crew bullet. Crew composition now uses ordinary conversation: the standing instructions cover proposing before working, custom members, direct collaboration, and inbox approval for additions; the retired `/crew` composer injection is removed.
9. SQ1's first-run gate, visible Squadron controls, and authenticated J5 route aggregate: `apps/web/src/routes/_chat.index.tsx:12-53` imports J5-owned gate/directory logic and wraps only the non-hosted-static `IndexDraftLanding` return; the hosted-static onboarding branch at `:30-32` remains unchanged. The gate's state is a real authenticated `GET /api/j5/squadrons` read per connected environment (case 34), not a hard-coded unavailable state; an unavailable read exposes only a forced retry, never an inferred home. `apps/web/src/components/Sidebar.tsx` mounts upstream `SidebarThreadHeader` and passes `<SquadronScopeDropdown variant="header" createOpen onCreateOpenChange/>` as its `projectScope`; its trigger is upstream `SidebarHeaderIconButton`, icon-only for All Squadrons and at the sidebar `size="sm"` 28px variant with a width-capped, truncated name and a full-name tooltip once scoped; that is the sole sidebar-zone dropdown mount and J5-owned scope consumer. Upstream's project-scope combobox and the row menu's Filter by project item are dropped (`projectFilter: null`). It does not alter chrome, footer, inbox, or roster. `apps/web/src/j5/squadron/SquadronCreateForm.tsx` is shared by the first-run gate and the J5-only ScopeDropdown `Create Squadron…` action: both require explicit name plus one selected folder on a connected environment whose session may operate (case 34; originally primary-environment only), POST that environment's route, refresh its directory, then select only the created Squadron. `apps/web/src/j5/squadron/SquadronDraftState.ts:9-18,43-50,76-83` retains scalar ambient scope semantics and increments a separate selection generation on every explicit choice, including Alpha→Alpha. `apps/web/src/j5/squadron/ThreadHomesClient.ts:109-121,132-145` reads B6's opaque batch `threadId → Registrar-home` response unchanged; Sidebar alone supplies that generation to reread its current raw thread ids for a named scope (zoom-out never forces), replacing stale `unknown` or failed/missing entries with durable Registrar homes. Its pure predicate admits only known homes matching a selected Squadron, excludes unknown/native rows, and the explicit `All Squadrons` state remains zoomed-out/all rows; it never proxies through `projectId`. `apps/web/src/components/ChatView.tsx:249-261,1552-1554,1812-1830,5225-5247,5437-5462,6579-6587` reads the active server thread through that opaque response; a known durable home takes precedence for a visible disabled chip, while unknown/native/legacy threads have none. The draft chip and `DraftHeroHeadline` name only the first-send carrier (durable home, else the draft's explicit choice) through J5 `resolveEffectiveSquadronId` in `SquadronScope.logic.ts`; ambient scope is never shown as the draft's Squadron, because send refuses it. Its J5-owned first-send boundary uses the known durable home or the draft-local explicit carrier, freezes only the draft-local carrier before the initial RPC, and never promotes ambient context into a launch identity. `apps/web/src/components/chat/DraftHeroHeadline.tsx:1-10` is display-only. `apps/web/src/components/chat/ChatComposer.tsx` changes only the `projectSelectionRequired` placeholder noun, "Choose a Squadron above to start a thread" (upstream: project), because the choice above the draft composer is the J5 Squadron picker. State, pure predicates, and UI stay in `apps/web/src/j5/squadron/`; no first choice is inferred. In `apps/server/src/server.ts:78-79,337-342,427-449`, `j5AuthenticatedRoutesLayer` is the one replacement for A4's raw route entry after `websocketRpcRouteLayer`, and the core J5 creation layer is provided once to the V2 runtime. The J5-owned aggregate in `apps/server/src/j5/a2a/J5AuthenticatedRoutes.ts:7-15` composes A4's human inbox with SQ1's Squadron list/create routes and B6's thread-home read. Future J5 authenticated route layers enter through this aggregate, never through another `server.ts` entry. On every rebase, verify these web mounts still have these boundaries, no project proxy, and the single server entry still follows `websocketRpcRouteLayer`.
10. SQ1's lossless launch carrier and durable attach boundary: `packages/contracts/src/orchestrationV2.ts:2322-2342` adds additive-optional unbranded `squadronId` at `:2325`; `packages/client-runtime/src/operations/commands.ts:144-167,546-612` carries it only from an explicit first-message caller into the launch RPC and loudly rejects the otherwise-silent no-bootstrap retry with the typed `SquadronLaunchRequiresBootstrapError`, directing the user to start a new thread. Bootstrap re-carry for that rare partial-failure recovery remains a queued improvement, not an invented fallback. `apps/server/src/ws.ts` forwards it only when present before the fixed `creationSource`; and `apps/server/src/orchestration-v2/ThreadLaunchService.ts:63-79,545-611` sends the carrier to J5's shared creation engine only after the thread is durable and before preparation is scheduled. The shared engine is provided once in `apps/server/src/server.ts:337-342`; the boundary rejects absence or invalid references without defaults, preserves the named durable orphan on failure, and retry uses the same deterministic registration command. On every rebase, verify this exact client → contract → WebSocket → launch → J5-engine chain, one runtime provider, and no parallel HTTP launch door.
11. SQ1's plan-provenance launch carrier is its own protected contract exception: `packages/contracts/src/orchestrationV2.ts:2322-2327` adds additive-optional `sourcePlanRef`; it is not an existing generic field and has no default. `packages/client-runtime/src/operations/commands.ts:152-167,590-596,624-629,669-683`, `apps/server/src/ws.ts`, and `apps/server/src/orchestration-v2/ThreadLaunchService.ts:63-79,548-592` pass it only from the plan→implementation launch. After durable child identity, ThreadLaunch reads the parent Registrar home through the shared engine: a known home is inherited; a legacy no-home parent remains the named native cohort, with neither refusal nor default. On every rebase, verify this additive contract field and the durable-before-preparation order.
12. SQ1's DV5 scheduled-new-thread refusal: `apps/server/src/scheduledTasks/ScheduledTaskService.ts:31-33,161-167,475-502` uses J5's named policy to refuse an unbound scheduled creation before `ThreadLaunch`, persisting a visible task failure rather than inventing scheduler provenance or a Squadron. `apps/server/src/scheduledTasks/ScheduledTaskService.test.ts` proves both actual stored `user/web` and `agent/mcp` schedules become visible failures. Upstream's scheduler dispatch tests seed bound tasks in J5 (their `insertRow` sets `thread_id` and they mock `ThreadManagement.sendToThread`) because unbound runs are refused; `ScheduledTaskService.layer` now needs `Scheduler`. On every rebase, verify the scheduled path remains an explicit unsupported/refused return pending scheduling-context work.

13. SQ1's opt-in primary-first Squadron folder-selection seam (case 42 separately carries catalog sources): `apps/web/src/commandPaletteBus.ts:7-31` adds the optional `onProjectSelected` carrier and its explicit durable project/folder payload; `apps/web/src/components/CommandPalette.tsx:393-585,1692-1809` retains that carrier only for an opt-in `add-project` open, threads it to the picker completion, returns an existing or newly created project before its normal navigation branch, and clears it on close. When absent, every existing Add Project caller retains its normal navigation behavior. `apps/web/src/j5/squadron/SquadronCreateForm.tsx:16-101` is the only SQ1 caller, reached from the first-run gate, the ScopeDropdown `Create Squadron…` item, and the sidebar header's `New Squadron` button (case 16): it presents name before folder, uses the ordinary picker, displays the returned human title and path rather than a UUID, and creates the Squadron before first-run landing opens a thread. The background clone (`startProjectClone`, `projectCloneTracking` servers) returns the folder through `returnCommandPaletteProjectSelection` after `waitForProject` and before any `handleNewThread`; the foreground clone reaches the carrier through `handleAddProject`. Upstream's other Add Project doors (the command palette action, `NoProjectsHero`, `PullRequestListEmptyState`) keep the absent-carrier default. On every rebase, verify both protected upstream anchors retain the absent-carrier default and that J5 still has no inferred folder or Squadron home.

14. B7's outbound A2A card seam: `apps/web/src/components/chat/MessagesTimeline.tsx:47` extends the existing J5 renderer import and `SimpleWorkEntryRow` delegates completed work entries to `renderThreadA2AOutboundTool` before its generic rendering, and `TimelineRowContent` delegates the `work-live` branch inside upstream's `WorkLogBlock` (`renderThreadA2AOutboundTool(row.entry) ?? <LiveWorkEntryTimelineRow/>`). Upstream moved live rows into the work-log block, so there is exactly one live-row render; preserve both current calls. `MessagesTimeline.test.tsx` covers the integrated sender/outbound rendering. The J5-owned delegate recognizes only completed, structurally valid `t3-code.send_message` / `mcp__t3-code__send_message` dynamic tools; non-send and malformed tools return `null` and keep generic rendering. This explicitly supports Codex's `t3-code.*` and Claude's `mcp__t3-code__*` structured dynamic-tool records. Cursor, Grok, and OpenCode are intentionally not supported by this treatment until their tool aliases and result shapes are independently verified; they retain generic tool rendering. It does not infer exchange pairs or create synthetic messages.

15. DQ5's server-home rebrand boundary: `apps/server/src/os-jank.ts` changes only the upstream `resolveBaseDir(undefined)` fallback from `~/.t3` to `~/.j5code`; `apps/server/src/cli/config.ts` continues to resolve `--base-dir` > `J5CODE_HOME` > desktop bootstrap > this default, and `apps/server/src/cli/pair.ts` reaches the same helper. `scripts/dev-runner.ts` keeps linked-worktree `.j5code` isolation and updates only its outside-worktree fallback/log presentation. The SSH remote launch/pair templates in `packages/ssh/src/tunnel.ts` use `~/.j5code` for both release-archive runtimes (unpacked under `$HOME/.j5code/runtime/versions`, case 40) and development `nodeScriptPath` runners; transport bookkeeping under `~/.t3/ssh-launch` remains separate, because moving it would orphan a running 0.0.42 remote server and start a second server on the same `~/.j5code` database. Migration utilities and `devDeliverySeed` retain explicit `~/.t3` source/safety paths because they protect or copy the installed upstream T3 database; their guards additionally reject J5's `~/.j5code` live state. Focused default/precedence, runner-identity, and safety-guard tests are the regression proof. This is a Jackson-authorized protected upstream edit; do not refactor surrounding code.

`apps/web/src/routeTree.gen.ts` is generated output, not a hand-authored exception. When a J5-owned route file changes, regenerate it with the normal web build and review only the generated route registration delta; never edit the generated tree directly.

15b. E7's Registrar-keyed command-palette context picker: `apps/web/src/components/CommandPalette.logic.ts:293-398` keeps the pre-E7 project search as its default/negative control and adds an opt-in Squadron search group plus the pure exact-Registrar-home destination resolver. `apps/web/src/components/CommandPalette.tsx:159-166,608-609,950-1018,1384-1442,1582-1589,1653-1687` mounts J5's directory/home reader, derives entries only from that directory, renders folders only as attributes, and uses the existing draft carrier after a folder starts a draft. Existing-thread navigation admits only the selected immutable home; a shared folder with Alpha and Bravo therefore never sends Bravo to Alpha's thread. The `onProjectSelected` carrier remains SQ1's folder picker, not a context chooser. Case 40 adds a separate source-selection carrier without changing SQ1 completion or normal Add Project behavior. A folder without a registered primary Squadron returns a named failure, never a default. Rebased and reverified against `origin/j5/main` `4dde2f6994fe2491fcd37d663a41895bacad0764` at this case's final head.
Landing-order collision: two cases landed as `15.`; resolved by the `b` suffix on 2026-09-04 without renumbering cases 16–23 or any existing cross-reference.

16. E7's sidebar new-thread route: `apps/web/src/components/Sidebar.tsx:138-148,1888-1900,3272-3303,3707-3743` lets exactly one ready Registrar Squadron retain direct creation; multiple Squadrons open case 15's picker, while ready-with-zero opens the existing J5 Create Squadron dialog rather than an empty picker. CodeRabbit ruling #193 keeps the empty state loading-only until the Registrar read is ready; only ready-with-zero shows “No Squadrons yet” and Create Squadron. Project count and shift-click cannot bypass a shared-folder choice. The header's add-folder button (upstream `onNewProject`) opens the existing J5 `SquadronCreateDialog` through `SquadronScopeDropdown`'s controlled `createOpen` pair instead of upstream's Add Project palette, so it never opens a Squadron-less draft; `Sidebar.tsx` drops the now-unused `openAddProjectCommandPalette`. Shift-click is disabled by passing `showNewThreadInProjectHint={false}` / `newThreadInProjectShortcutLabel={null}` to upstream `SidebarThreadHeader`, whose add-folder button label is `New Squadron` (one-string edit in `components/sidebar/SidebarThreadHeader.tsx`). This is outside the B1 chrome/bell grid, SQ1 scope dropdown, and Fleet roster zones.

17. E7's command-palette empty-result copy: `apps/web/src/components/CommandPaletteResults.tsx:91-101` changes only the visible `projects` noun at `:98` to `Squadrons`; mechanics and all other copy remain byte-identical.

18. E7's Squadron-creation folder copy: `apps/web/src/j5/squadron/SquadronCreateForm.tsx:68-90` changes only the two string bodies at `:72` and `:84` from project-picker wording to folder wording, and under the post-review truthful-label ruling makes the `aria-label` at `:72` match the control's visible Choose folder / Change folder text. `SquadronCreate.logic.ts` and the `onProjectSelected` mechanics/type discriminants remain unchanged.

19. E7's remaining new-thread doors: `apps/web/src/routes/_chat.tsx:1-162` makes both keyboard new-thread commands choose the sole ready Registrar entry through SQ1's draft carrier or open the Squadron picker; project cardinality is absent, and a sole missing folder fails closed with a named error. `apps/web/src/components/Sidebar.tsx:1893-1923,3040-3085,3281-3315,3707-3743` uses current Registrar snapshots for both its toolbar and its in-reach `new-thread-on-branch` context-menu action: the branch action resolves the source immutable home, uses that entry's folder only as launch substrate, and passes SQ1's carrier; unknown/multi opens the picker and a missing folder fails closed with a named error. It also opens the existing J5 `SquadronCreateDialog`, for ready-with-zero new-thread and the header's `New Squadron` button (case 16), through a controlled `createOpen` / `onCreateOpenChange` prop pair in `apps/web/src/j5/squadron/SquadronScopeDropdown.tsx`; its type prevents passing one without the other, and no second form exists. **RULE-15 reachability proof:** the E7-converted `new-thread-on-branch` trace has exactly the shared menu builder plus this Sidebar dispatcher and the header menu below; the separately boarded `LegacySidebar` is recorded in the inventory. `apps/web/src/hooks/useThreadActionMenu.ts` is upstream-owned again and mounted by `ChatHeader`; do not delete it. Its `new-thread-on-branch` calls J5 `useSquadronNewThreadOnBranch` (`apps/web/src/j5/squadron/useSquadronNewThreadOnBranch.ts`), whose `startSquadronThreadOnBranch` the Sidebar row menu shares, and its `archive` runs case 21's `archiveWithPreflight`. `apps/web/src/components/ChatView.tsx:1811-1870,6458-6470` makes the current-thread header action preserve its known immutable home, or on a draft the Squadron the draft has explicitly chosen (J5 `resolveHeaderSquadronRef` in `SquadronPicker.logic.ts`), or fall through only to the ready/exact-one Registrar choice; the header names that Squadron and its button launches a new thread with that entry's folder and SQ1 carrier, never the active folder. `apps/web/src/components/chat/ChatHeader.tsx:8-78` displays the determined Squadron as the action identity and the active folder only as a subordinate attribute. `apps/web/src/routes/_chat.index.tsx:61-190` auto-launches only an ambient-selected or exact-one Registrar entry through the same carrier; multi stays in an explicit index choice state whose user action opens the existing picker, while zero stays in case 9's existing first-run creation control. A missing selected folder is a permanent honest state; only a transient launch failure retries. `apps/web/src/components/CommandPalette.tsx:1-1018,1653-1768` tracks resolved Registrar entries in every derived menu/callback; `apps/web/src/components/Sidebar.tsx`, `apps/web/src/routes/_chat.tsx`, `apps/web/src/components/ChatView.tsx`, and `apps/web/src/routes/_chat.index.tsx` all pass SQ1's carrier to `apps/web/src/j5/squadron/SquadronPicker.logic.ts`; its single `squadronDraftScopeKey(environmentId, { draftId, threadId })` stores only under `scopedThreadKey(scopeThreadRef(environmentId, threadId))`, never `draftId`. The branch action consumes the source immutable home and fails closed while unavailable; it never derives a home from `projectId`. `apps/web/src/components/Sidebar.logic.ts` deletes its private, obsolete project-cardinality helper, and `apps/web/src/components/ChatView.logic.ts` deletes its measured-dead project launch helper and focused tests. `startNewThreadFromContext` is retained as an upstream-exported generic utility with its focused contract tests; it has no Squadron-gated callers. `apps/web/src/components/pullRequest/PullRequestListEmptyState.tsx:109-116` is copy-only folder wording; `CommandPalette.tsx:985-1007` removes an unused callback dependency. Multi-model fan-out (`ChatView` `multipleModelSelections`) passes the first-send `squadronId` to every `startThreadTurn` and refuses fan-out for a saved-agent draft (decision #7b, 2026-09-24: a persona pins one immutable route, so carrying the persona would run every fan-out thread on that one model); the background send's fresh draft inherits the sent Squadron via `selectDraftSquadron`. Upstream's `ProjectCloneToastCoordinator` Open action keeps the Add Project absent-carrier default; the first-send carrier refuses to launch without a Squadron. On every rebase, verify no raw project-derived launch remains on these Squadron-gated doors, the shared carrier key retains its returned scoped thread id, Registrar-derived views react to snapshots, and no second Squadron creation surface is introduced.

20. E7's revised Squadron-picker row shape: `apps/web/src/components/CommandPalette.tsx:990-1004` delegates its row fields to J5-owned `apps/web/src/j5/squadron/SquadronPicker.logic.ts:18-23`. The visible row is the Squadron name alone and has no folder-description second line; folder title/path remain search terms, availability state, and launch substrate only. Amended by case 34 (Jackson's ruling, 2026-09-12): once the directory spans more than one environment, the row's description is the owning environment's label, because two environments can each hold a Squadron of the same name; a single-environment directory keeps the name-only row. The row helper's focused tests prove both the retained non-visual mechanics, the absence of a `description` field for one environment, and the label for two. On every rebase, preserve this presentation without changing the Registrar-derived identity or the missing-folder failure behavior.

21. B4's archive preflight is a J5-owned, per-thread read before every web archive door: `apps/web/src/j5/a2a/archiveFlow.ts` carries the complete `ScopedThreadRef`, and `archiveFlowClient.ts` resolves that ref's environment before its authenticated `/api/j5/a2a/pre-archive-facts` POST; it must never fall back to the primary environment. `apps/server/src/j5/a2a/J5AuthenticatedRoutes.ts` composes the read through the one authenticated J5 aggregate, while `runtimeLayer.ts` supplies the real `placementFactsLayer` alongside A2S's `archiveAgentLayer`. `ArchiveFactsService` reports a failed placement read explicitly as unknown, and the web warning renders a failed preflight as “Couldn't check” rather than a reassuring empty list. `Sidebar.tsx` and `LegacySidebar.tsx` route their direct, bulk, and hover archive paths through this same flow: consequential threads get the measured destructive warning; clean threads preserve their existing generic confirmation behavior, with batch cancellation and unresolved selection recovery retained. Mobile archive doors remain explicitly deferred. Since 2026-09-15 the preflight read also attaches the thread's Crew relations with no further Sidebar edit: the live Crews its agent commands (`ArchiveCrewService.readCaptainFacts`), which the warning lists seat by seat, and the seat it holds, which `archiveWithPreflight` refuses to archive alone up front; the rule itself is on the server since case 37. The Captain rule is enforced server-side: J5-owned `CrewCaptainArchiveCascade.ts` reads the same stored-event stream as the seat finish notifier and retires a Captain's live Crews as units when its `thread.archived` or `thread.deleted` commits, whichever door the archive came through; the person's own Archive crew on the Fleet header uses the J5 route `POST /api/j5/a2a/crews/archive` (`CrewArchiveHttp.ts`, operate scope). Both reactions ride a stream that resumes from its high-water mark and log rather than retry a failure, so the notifier daemon runs one sweep before streaming (2026-09-17, Bryant's choice after Jackson's review of PR #151): `CrewCaptainArchiveCascade.reconcile` retires every live Crew whose Captain thread is archived or gone, and `CrewSeatFinishNotifier.reconcile` tells the Captain of every live seat whose newest run finished with nothing reporting it. `archiveWithPreflight` passes `{ undoable }` to its `archive` callback; `archiveMayRetireCrews` is false only for a clean or non-Captain thread with a successful Crew read. `useThreadActions.archiveThread` takes optional `undoable` and skips upstream's Undo notice (#12848) when false, because `thread.unarchived` does not restore retired Crews (decision #7, 2026-09-24). Doors: Sidebar row menu, header menu (`useThreadActionMenu`), LegacySidebar. On every rebase, verify the web call sites still carry the scoped environment through the client seam, the archive case remains wrapped, and unknown reads never become clean facts.

22. B4's typed confirmation presentation seam: `apps/web/src/confirmDialog.ts:4-31,92-158` extends only the in-memory `requestConfirmDialog(message, options, presentation?)` coordinator state with optional `ReactNode` content and `confirmLabel`; `apps/web/src/components/ConfirmDialogHost.tsx:57-67,79-116` renders the typed node and label when present and otherwise keeps its existing string title/description plus `Confirm` path. The host has no J5 import, payload decoding, or private prefix branch. J5 owns the archive row content in `apps/web/src/j5/a2a/ArchiveWarningContent.tsx:6-130`; `apps/web/src/j5/a2a/archiveFlow.ts:13-17,40-94` builds and passes it for consequential archive facts, with `Archive anyway` only when one or more open asks are measured and `Archive` otherwise. `apps/web/src/components/Sidebar.tsx:3169-3181` plus `apps/web/src/components/LegacySidebar.tsx:1824-1836,2012-2018` use the existing confirmation coordinator to carry the presentation. Existing string callers omit the third argument and remain byte-for-byte on the prior path. Jackson's Variant B title `Archive <agent>?` supersedes the earlier Designer condition that put “anyway” in the question; the explicit button label now disambiguates the action. On every rebase, verify the host stays J5-agnostic, the absent-presentation string branch remains intact, and the three archive doors continue to pass J5-owned presentation only for fact-backed warnings.

23. Issue #47's Registrar-first thread-card label: `apps/web/src/components/Sidebar.tsx:157-158,729,1370-1380,3614-3620` only imports and mounts J5-owned `apps/web/src/j5/squadron/ThreadCardIdentity.tsx:8-13`, passing the already-reactive `useThreadHomes` result to a full-card row. `ThreadHomesClient.ts:50-71` resolves each canonical Registrar participant id through B6's existing authenticated `POST /api/j5/a2a/client-reads/participant-homes` read, then maps its response back to the opaque thread id; it neither creates a server surface nor displays a participant id. A known home renders its Squadron name above the thread/agent title; unknown or native home retains the previous folder label. The 2026-09-09 Crew slice adds one prop to that mount, `threadId={thread.id}`, so J5-owned `ThreadCardIdentity` can read `CrewMembershipsClient.ts` (an external store fed by `useThreadHomes` from the same visible-row set, over the authenticated `POST /api/j5/a2a/client-reads/crew-memberships` read registered in `J5AuthenticatedRoutes.ts`) and render a seat chip on Crew members and a Captain chip on launchers; the list stays flat by recency and no grouping is introduced. SB5 membership is enforced entirely inside J5-owned `SquadronScope.logic.ts` (`isSidebarMember`, applied by the already-called `filterThreadsForSquadronScope`), fed by the optional `origin` the J5 participant-homes read now states from placement provenance; agent-spawned Peer Agents therefore leave the sidebar unless pinned without any further Sidebar edit. The Roster that keeps them visible is the J5-owned `/fleet` page over `POST /api/j5/a2a/client-reads/fleet`. The same card also mounts J5-owned `SpawnedChildren` once, immediately before its closing `</li>`, which renders a collapsed expander of the row's placed children from `SpawnedChildrenClient.ts` (`POST /api/j5/a2a/client-reads/spawned-children`, requested alongside homes); the nested rows are J5-rendered and never reuse the upstream card component. Both per-thread stores (`createScopedThreadReadStore` in `packages/client-runtime/src/j5/threadHomes.ts`) keep an empty answer loaded so a reordered row set never re-reads it, keep the previous snapshot when a batch changes nothing so rows skip their render, and the 30-second Fleet poll re-reads only the rows the roster names as involved (`fleetInvolvedThreadRefs`: seats, their Captains, spawners with placed children), so those reads follow Crew activity rather than the length of the thread list (2026-09-17, after Jackson's review of PR #149). The expander's remembered expansion is keyed by environment, parent, and group. The existing tooltip remains the secondary detail for folder, machine, and model. The thin Sidebar integration must remain card-only: search and slim rows are byte-for-byte unchanged. On every rebase, preserve the honest no-home fallback and make two Squadrons sharing one folder distinguishable by their immutable home names rather than project metadata. Since 2026-09-14 the `ThreadCardIdentity` mount also passes `environmentId`, so the Crew chip is read from the thread's own environment rather than the primary one.

24. N42/N62's single participant-identity rendering seam: `apps/web/src/components/chat/MessagesTimeline.tsx:47-52,316-323,1054-1063` imports only J5-owned identity helpers, batches eligible peer sender ids from actual timeline entries, and passes that identity-read result only into the established `renderThreadA2ADelivery` delegate. `apps/web/src/components/chat/QueuedRunsControl.tsx:16-20,68-71,148-149,214-221` reuses that exact delegate's envelope parser and formatter for queued rows; it shows `From <sender name> — <first body line>` and leaves a durable id only on the unknown-name tooltip. The reachability proof is the sole active-thread `ChatView` mounts: `apps/web/src/components/ChatView.tsx:6501-6515` mounts `MessagesTimeline`, and `:6597-6603` mounts `QueuedRunsControl` only for a server thread. Both upstream hunks therefore reach J5-owned `ParticipantIdentitiesClient.ts`, `ParticipantIdentity.ts`, and `ThreadA2ARenderer.tsx`; generic timeline/queue text retains its byte-for-byte fallback. On every rebase, preserve the two thin delegates and these exact mounts rather than embedding another renderer in either upstream component. The human-authored minimap filter is applied in upstream’s extracted `apps/web/src/components/chat/timelineMinimapItems.ts`, using the J5 message classifier; do not restore the old derivation in `MessagesTimeline.logic.ts`.

25. Issue #68's on-disk separation boundary (Jackson's 2026-09-02 ruling: J5 never shares on-disk locations or environment names with an installed upstream T3 Code). The base-directory variable is `J5CODE_HOME` everywhere it is read or written, with no fallback read of `T3CODE_HOME` anywhere: `apps/server/src/cli/config.ts:35,107` (flag description and the env read behind `--base-dir` > `J5CODE_HOME` > desktop bootstrap > `~/.j5code`), `apps/server/src/cli/pair.ts:257-264` (worktree home first, then `J5CODE_HOME`), `apps/server/src/serviceLauncher.ts` `main()` (the required variable; now exported and hosted by the executable's `__service-launcher` subcommand), `apps/server/src/cloud/bootService.ts` (both `renderBootServiceUnit`'s systemd `Environment=` line and `renderBootServicePlist`'s launchd environment dictionary, and `bootServiceBaseDirOf`, which parses them back; case 41), `scripts/dev-runner.ts:323-340,676-706,863` (child export and precedence `--home-dir` > worktree dir > ambient `J5CODE_HOME`), and the desktop's reads: `apps/desktop/src/app/DesktopConfig.ts:39` (the primary read feeding `DesktopEnvironment.ts` base/state dirs and the backend bootstrap), `apps/desktop/src/app/DesktopEarlyElectronStartup.ts:54` (the pre-Effect early read), `apps/desktop/scripts/electron-launcher.mjs:117` (dev launcher forward), and `apps/desktop/src/backend/DesktopBackendConfiguration.ts:526-533` (the WSL strip of the same name). `apps/desktop/src/wsl/DesktopWslEnvironment.ts` places the WSL runtime under `$HOME/.j5code/wsl-runtime`. `apps/desktop/src/app/DesktopUserData.ts` `resolveUserDataPath` keeps `j5code`/`j5code-dev` (or an existing `J5 Code`/`J5 Code (Dev)`) on every platform (Jackson's 2026-09-24 decision #4); upstream's `t3code-v2` production profile and Windows `Local State` seeding from T3 profiles are deliberately not adopted, so J5 never chooses or reads a T3 profile and future upstream changes to that file will conflict. The linked-worktree state directory is `<worktree>/.j5code`: `packages/shared/src/devHome.ts:102` (`resolveWorktreeT3Home`; the upstream function and `t3Home` parameter names are deliberately kept as generic base-dir vocabulary), `.gitignore` and `vite.config.ts` test `exclude` each add a `.j5code` line beside upstream's `.t3` line (add-beside; the `.t3` lines are upstream's own). `apps/server/src/cli/app.ts`, `theme.ts`, and `triage.ts` also read only `J5CODE_HOME`; their upstream internal symbol names remain unchanged. `apps/server/scripts/migrate-dev-db.ts` follows `resolveWorktreeT3Home`, updates comments/help, defaults its copy source to `statev2.sqlite` in the shared home when present (else `state.sqlite`, the pre-V2 file), and adds behavioral guards against mutating either canonical shared home, including when a custom shared-home option is supplied. `apps/server/scripts/t3-sqlite-state.ts` applies the same canonical-home protection to `exec`. Both target `<base>/userdata/statev2.sqlite`; their shared-home guards are directory-level and therefore protect `statev2.sqlite`, `state.sqlite` and their `-wal`/`-shm` sidecars. Their deliberate `~/.t3` source/protection paths remain beside `~/.j5code` per case 15; focused tests cover the guards. Pair discovery has additional behavior recorded in the review inventory below. `apps/server/src/telemetry/Identify.ts:252` is a comment correction only: the anonymous id already lives at `<stateDir>/anonymous-id` under the J5 base dir. Proofs of record: `apps/server/src/cli/config.test.ts`, test `ignores an ambient T3CODE_HOME so an installed T3 Code never leaks in` (the twin of the retained `uses ~/.j5code for a bare server without touching ~/.t3`), the `scripts/dev-runner.test.ts` precedence suite, `packages/shared/src/devHome.test.ts`, and the `Environment=J5CODE_HOME=` assertion in `apps/server/src/cloud/bootService.test.ts`. Desktop test fixtures key their env on `J5CODE_HOME`; `bootService.test.ts` and `cli/service.test.ts` keep `.t3` paths as fixture inputs only. The ~30 cosmetic `T3CODE_*` settings are deferred to the naming audit. Upstream prose that still names `.t3` or `T3CODE_HOME` (`.agents/skills/*`, `docs/internals`, `docs/operations`, `.cursor`) is untouched by rule; `AGENTS.md`'s live-install rule, dev-server home and test-data snippet are the one exception (J5 home `.j5code`, `J5CODE_HOME`, `statev2.sqlite`/`state.sqlite` and a `node:sqlite` `VACUUM INTO`), because agents follow them literally against live data; `packages/ssh/src/tunnel.ts` keeps only its `~/.t3/ssh-launch` transport-bookkeeping path per case 15. On every rebase, verify no read of `T3CODE_HOME` returns, the worktree default still outranks the ambient variable, and the unit writer and launcher reader name the same variable.
26. Active-Astra peer delivery and durable steer outcomes: `apps/server/src/j5/a2a/DeliveryTransport.ts` sends eligible running Codex Astra peer updates as run-pinned native steers. Other models and system notices queue. `EffectOutbox.ts` retains J5 `awaitSettled`; the runtime supplies that same outbox instance to delivery. Adopt upstream completed-turn follow-up with the original message identity and model: it queues behind active work and is delivered once durably accepted, even when no steer adapter call occurs. Transport verifies either the original steer outcome or a durable run for the same message. Interrupted, failed and explicitly stopped work does not follow up. The committed-Stop steer patch below closes both steer races. This is independent of upstream human composer defaults. Upstream holds queued runs after restart and queues new messages held behind them (decision #6, 2026-09-24). A delivery whose accepted run is `queued` + `queueHeld` is not delivered: `DeliveryTransport` fails it with `A2ADeliveryHeldError` on both the queue path and the completed-steer follow-up path, and `DeliveryWorker` never alarms on it, recording a `retry_scheduled` recheck at a doubling interval from 1 to at most 15 minutes, which bounds ledger growth while a queue stays held. After someone resumes the thread, the next recheck writes `message.delivered` once, reusing the same durable message. Deliveries already marked delivered before a restart are not re-reported. Peer deliveries from agents carry `senderThreadId`, read by primary key from the sender's membership row in its home Squadron. Tests: `DeliveryTransport.integration.test.ts` "keeps held delivery pending…" and "settles a held peer delivery once after resume without alarming", and the `DeliveryWorker.test.ts` held case.
27. Retired 2026-09-24 (Jackson's review of PR #262): issue #73's truthful-steer seam is removed, and the person's steer and queue presentation follows upstream as-is on every client. `packages/client-runtime/src/j5/steerState.ts`, its `./j5/steer-state` export, and `apps/web/src/j5/composer/` are deleted; `ComposerPrimaryActions.tsx` and mobile `ThreadQueueControl.tsx` equal upstream; `ChatComposer.tsx` and `QueuedRunsControl.tsx` keep only their other recorded seams (saved-agent/persona rows, case 24). Remaining steer UX gaps are tracked in the upstream give-back backlog, [Jacksondr5/j5code#276](https://github.com/Jacksondr5/j5code/issues/276). The number stays reserved.

28. Issue #90's Codex CLI version gate: `apps/server/src/orchestration-v2/Adapters/CodexAdapterV2.ts:50,1444-1450` passes the `initialize` response's `userAgent` to J5-owned `apps/server/src/j5/codex/CodexCliVersionGate.ts`, which fails closed with `J5 requires Codex CLI ≥ 0.151.0; found X` before any thread request reaches an app-server below the configured minimum, or one reporting an unparseable user agent. `apps/server/src/orchestration-v2/ProviderTurnStartService.ts:24,26,192,245-248,343-368` recognizes that error in the thread load failure, refuses the portable-context fallback for it, and hands the run to run execution with a `startTurn` that fails with the named message, so the run fails visibly through the existing failure path instead of being retried by the effect worker. No compatibility shim exists. Revalidate the support floor with every schema regen. This advance retained the intended minimum `0.151.0` while generating from `0.152.1`; its normalized `0.152.1` fixtures do not prove real `0.151.0` wire compatibility. That lower-bound evidence remains a final review decision, not a demonstrated incompatibility or an automatic reason to raise the floor. This guard is J5 policy and stays after upstream fixes its schema; it is not one of the temporary in-place patches below.

29. Retired 2026-09-24 with case 27: the draft subscription guard existed only for the `useJ5SteerState` subscription. No J5 hook in `ChatComposer.tsx` now reads a server thread by id before the draft becomes a server thread (the saved-agent mention picker and draft-agent assignment read local and environment-wide state; the persona assignment control mounts only for an existing assigned thread), so the guard and its regression test are removed. Reinstate a guard at any future J5 delegate that subscribes to the active thread from the composer. The number stays reserved.

30. Issue #64's queued-run observation composition: only `apps/server/src/orchestration-v2/runtimeLayer.ts`, `ProviderTurnStartService.ts`, and `RunFinalizationService.ts` contain new upstream delegates. The runtime supplies one J5 watchdog and worker; start/finalization pass failures to the J5 observer. All candidate SQL, cursor pagination, VCS cause-chain filtering, redaction, deduplication, and best-effort reporting live in `apps/server/src/j5/run-observability/`. Its migration runs when the candidate service is initialized after upstream persistence, uses `j5_run_observability_migrations` and a J5-prefixed index, and consumes no upstream migration IDs. The read service depends on upstream's `orchestration_v2_projection_runs` and `orchestration_v2_projection_threads` schema; verify those columns and the `(status, requested_at, run_id)` query plan on every rebase. No new projection-store API or upstream test/persistence edits are needed. After five minutes, bounded scans fairly traverse starting runs and write a deterministic completed error-kind waiting fact through the current-run guard. VCS observations unwrap nested causes inside J5 and never replace the original failure. Preserve one worker, guarded writes, historical facts, and the no-retry/no-terminalization boundary. J5-owned tests cover indexed pagination, migration-lane isolation, deduplication, wrapped checkpoint/refresh errors, and failed observation writes.

31. Upstream migration-history compatibility: `apps/server/src/j5/persistence/UpstreamMigrationCompatibility.ts` owns the pre-migration recognition, atomic upgrade and refusal policy, reached only through case 1. Current upstream prefixes use the ordinary upstream migrator. Three reviewed histories are upgraded: the pin `62aef8587c` history (1–50 plus `51 = OrchestrationV2`, which upstream renumbered to 54 after inserting 051–053) runs only upstream 051–053, moves the V2 row to 54 by UPDATE (keeping its timestamp), records 051–053, and lets the ordinary migrator run 055 in the same transaction; complete August histories and September prefixes through 048–059 run only their missing target SQL through 053 and remaining September V2 steps, then record the composed V2 identity at 54. V2 is never replayed over an existing V2 schema. Admission is exact (history and a frozen `sqlite_master` digest per source) and happens before any write; upstream T3 Code V2 preview databases (`53 = OrchestrationV2`) and all other histories are refused (Jackson's 2026-09-24 decision #1: J5 and T3 never share databases). Original rows are archived in `j5_upstream_migration_history` under a distinct `source_ref` per source (August `993407dd9`, September `b9fa1399c`, pin `62aef8587c`); reviewed manifests are `legacy-upstream-migrations.v1.json`, `reviewed-v2-collapse.v1.json` and `reviewed-v2-renumber.v1.json`, never regenerated from candidate code. Upstream's `reconcileV2PreviewMigration` and runtime helpers remain upstream-owned and unedited. The audit identifies the composed V2 and the 050 PR backfill by manifest name and fingerprints their bounded dependencies; 050's backfill helpers drifted at this advance (Forgejo `/pulls`, HTTP ports), which is safe only because a recorded 050 never reruns. J5 migration lanes are unchanged. `UpstreamMigrationCompatibility.test.ts`, `StateV2Cutover.test.ts` and `scripts/j5/check-upstream-migrations.test.ts` cover supported histories, refusals, injected rollback, idempotence, the statev2 copy and manifest drift; disposable real-snapshot upgrade proofs are required before rollout.

32. Astra model aliases: `packages/contracts/src/model.ts` maps `astra`, `gpt-6` and `6` to `gpt-6-astra`. These are explicit aliases, not a separate provider manifest or legacy classifier. `packages/shared/src/model.test.ts` checks their normalization. The current upstream model manifest and Fable 5 legacy classification remain unchanged.

33. Artifacts (PR #109, corrected 2026-09-14). Storage is server-owned application data, never the repository: `apps/server/src/j5/artifacts/ArtifactWorkspace.ts` keeps one directory per project under `<state dir>/artifacts/<sha256(projectId)>/`, so there is no Git exclusion and no per-thread folder. Production decorates the existing `RunFinalizationObserver` with J5-owned `ArtifactRunFinalizationObserver.ts` (wrapped in turn by the handoff observer), preserving both upstream refresh methods (`refreshAfterTurn` is now `(projectId) => Effect`; the decorators pass it through unchanged, nesting handoff → artifact → upstream). It reads the settled projection to export the final proposed plan to the project's `plan.md`; no observer argument extension is needed. `apps/server/src/j5/a2a/J5AuthenticatedRoutes.ts` appends the authenticated, project-addressed artifact list/read routes and the operate-scoped permanent-delete mutation to the sole J5 aggregate. Deletion uses non-recursive platform filesystem removal and adds no runtime dependency or packaging seam. Successful deletion reconciles a matching saved-agent handoff to `missing` and publishes the normal handoff refresh; reconciliation stays best-effort because the file has already been deleted and cannot be rolled back. The artifact change stream is a J5 RPC group, `J5ArtifactRpcGroup` in `packages/contracts/src/j5/artifacts.ts`, merged into `WsRpcGroup` in `packages/contracts/src/rpc.ts` beside the persona group (one appended `.merge`); `apps/server/src/auth/RpcAuthorization.ts` spreads its scopes and `apps/server/src/ws.ts` spreads its handlers from J5-owned `apps/server/src/j5/artifacts/artifactRpc.ts`, and `packages/client-runtime/src/rpc/client.ts` names its tag in the subscription-tag union. `apps/web/src/components/sidebar/SidebarChrome.tsx` adds the Artifacts footer icon and Back-page recognition; `rightPanelStore.ts`, `RightPanelTabs.tsx`, and `ChatView.tsx` add a project-scoped singleton right-panel surface that reuses the J5-owned artifact view; `rightPanelStore` storage version is 15 (J5 13 + upstream 14), and its version-agnostic migration drops `plan`/`agents` and normalizes `artifacts`. `ChatMarkdown.tsx` recognizes workspace-relative `artifacts/` file references, gives them distinct presentation, and routes their primary action to that surface with the referenced artifact selected. `apps/server/src/provider/T3OrchestrationInstructions.ts` adds one planning-document placement bullet. The MCP `write_artifact`, `read_artifact`, and `list_artifacts` tools (`apps/server/src/mcp/toolkits/artifacts/`, `ArtifactMcpService.ts`) are registered beside upstream's toolkits in `McpHttpServer.ts`. The route, client, project page, filesystem policy, path classifier, previews, and change subscription otherwise live in J5-owned files. The generated `apps/web/src/routeTree.gen.ts` delta is not hand-authored. On every rebase, keep plan export and post-delete handoff reconciliation best-effort so neither can poison a completed operation, preserve the finalization observer's original refresh behavior, and keep one shared project namespace.

34. Issue #105's cross-environment reads for Squadrons and the human inbox (read models merge in the client; authority stays on each server, per [`docs/j5/product/cross-device.md`](docs/j5/product/cross-device.md)): `packages/contracts/src/environment.ts` adds two optional capability keys, `j5Squadrons` and `j5HumanInbox`, beside upstream's existing optional capabilities, and `apps/server/src/environment/ServerEnvironment.ts` (with its test) reports both as true; a server that omits the keys is probed once on its read route, and a 404/501 marks that source unsupported. `packages/contracts/package.json` adds the `./j5` export for J5-owned `packages/contracts/src/j5.ts`; `packages/client-runtime/package.json` appends six `./j5/*` exports for the J5-owned read-source, HTTP, and merge modules under `packages/client-runtime/src/j5/`. Those modules reuse upstream's environment registry and per-environment query atoms. `packages/client-runtime/src/state/environmentHttpAuth.ts` exposes `executeAuthenticatedEnvironmentRawHttpRequest` for the J5 routes; upstream's typed-group `executeAuthenticatedEnvironmentHttpRequest` delegates to that same credential/retry implementation. This keeps cookie, bearer, and relay DPoP with one refresh-and-retry shared without assigning J5 routes to an unrelated upstream API group, and no J5 read falls back to the primary environment. The upstream mounts already recorded in cases 9, 15b, 16, 19, and 20 (`ChatView.tsx`, `CommandPalette.logic.ts`, `CommandPalette.tsx`, `Sidebar.logic.ts`, `Sidebar.tsx`, `routes/_chat.tsx`, `routes/_chat.index.tsx`) now carry `ScopedSquadronRef` / `ScopedThreadRef` identities instead of bare ids so two environments' Squadrons never collide; no new mount or door is added, and `_chat.index.tsx` keeps upstream's bootstrap wait before judging a Squadron's folder missing. Environment labels appear only once a read model spans more than one environment. A draft with a selected Squadron excludes automatic environment balancing and refuses manual environment changes; selecting another Squadron is the explicit owner-change route. The shared guard in `ChatView.logic.ts` is used by all three environment-selection doors in `ChatView.tsx`; a bare Squadron id is never copied to another environment. Native mobile screens are deferred. On every rebase, verify the capability keys stay optional, the J5 request helper still goes through the shared authenticated transport, and the scoped identities survive in each recorded mount.
    DV5 extends cases 9–12 without opening a second creation door. `apps/web/src/j5/squadron/FirstRunGate.tsx` creates a named Squadron only from the one explicitly selected folder, and state-names an unavailable or read-only environment refusal (originally a non-primary refusal; case 34 routes creation to the folder's own environment); `Sidebar.tsx` replaces the old project scope zone with a Squadron scope whose pure predicate filters the real thread list only by immutable Registrar home: selected scope excludes unknown/native, while the explicit unscoped state shows all rows. `DraftHeroHeadline.tsx` renders only `What should we build in ⟨Squadron⟩?`. The plan→implementation behavior is recorded separately in case 11, including its additive `sourcePlanRef` contract exception. On every rebase, verify the inherited-home lookup remains after durable child identity and before preparation, the Sidebar home read has no project proxy, and the separately named mobile, scheduled, system-bootstrap, and agent-spawn returns remain explicit in `SquadronLaunchPolicy.ts` and `docs/j5/product/dogfood-v0.md` DV5.

### Temporary in-place patches (watching upstream for a fix)

These are direct edits to upstream-owned files that exist only because upstream has a defect J5 cannot wait on. They are not integration cases and must not grow. Compare each patch individually; do not discard other still-required J5 changes in the same file. The pin-review checklist reads this section at every drift review: for each entry, check the watch signal against the candidate upstream diff. Exit for every entry is exact: **remove the matching superseded hunks at the rebase where upstream's equivalent fix lands**. Take the whole upstream file only when no other J5 policy or integration delta remains in it; a logging fix, for example, does not supersede the native-resume rule.

- **Command-palette thread identity across environments.** `apps/web/src/components/CommandPalette.logic.ts` qualifies recent/search thread values with the environment, matching the existing linked-thread helper. Identical raw thread ids on two servers otherwise produce duplicate React keys and ambiguous palette selection. A collision regression covers unique selection values and routed actions; upstream's two newer `CommandPalette.logic.test.ts` cases expect the qualified values. Watch signal: upstream qualifies these values by environment. Exit: remove this matching hunk and adopt upstream's equivalent fix.

- **Codex protocol schema regenerated at 0.152.1** (issue #90). `packages/effect-codex-app-server/scripts/generate.ts` keeps `UPSTREAM_REF` `5adb68a49933ae446bf11935662c83dba55a0804` (`rust-v0.152.1`), J5's `distributeObjectUnion` step, and upstream's async-question extension (including its `type`-last ordering); output under `src/_generated/` is committed unmodified. Effect rc.115's importer reads an object schema without `additionalProperties` as open and emits `StructWithRest`, which the hand-written client cannot extend; the generator's `closeObjectProperties` `onEnter` hook closes plain object schemas (never `oneOf`/`anyOf`/`allOf` roots), reproducing the rc.112 `Struct` output, which still ignores unknown keys when decoding. Watch signal: upstream bumps `UPSTREAM_REF` to ≥ `5adb68a` or regenerates under rc.115+. Exit: remove at the rebase where upstream's regen lands, keeping whatever normalization upstream adopts.
- **Protocol drift absorbed by the regen** (issue #90). `apps/server/src/orchestration-v2/Adapters/CodexAdapterV2.ts` returns the structured denied response and types thread configuration as JSON. `apps/server/src/provider/Layers/CodexSessionRuntime.ts` types elicitation defaults and response content as JSON; its tests cover numeric, boolean and array defaults. Protocol/client/adapter fixtures carry required 0.152.1 fields; `provider/Layers/ProviderInstanceRegistryLive.test.ts` expects the matching version reported by the shared Codex mock peer. Upstream already supplies the current account plan labels, so the previous local `CodexProvider.ts` label patch is retired. Watch signal: upstream generates equivalent required shapes and adapters accept them. Exit: remove each corresponding delta once supplied upstream, preserving any independent J5 policy.
- **Recorded Codex replay transcripts migrated to the 0.152.1 shape** (issue #90). Files: the 26 `apps/server/src/orchestration-v2/testkit/fixtures/*/codex_transcript.ndjson`, edited by `scripts/j5/migrate-codex-fixtures.mjs` (rules: version strings → 0.152.1; `thread` objects gain `sessionId` = id and `projectId: null`; `item/started`/approval requests gain `startedAtMs`, `item/completed` gains `completedAtMs`, constant 1756771200000; `requestUserInput` gains `isBlocking: true`; `remoteControl/status/changed` gains placeholder `installationId`/`serverName`; only missing keys are appended). Applying it to the previous pin's fixtures reproduces the fork's byte for byte; at each advance take upstream's transcripts and rerun it, never hand-merge. Upstream defect: the transcripts were recorded against Codex 0.120–0.141 and only decode under the stale schema. Watch signal: upstream re-records or migrates these fixtures alongside its regen. Exit: remove at the rebase where upstream's fix lands.
- **Native resume replaces history only when the provider says it is gone** (issue #90; Jackson's 2026-09-24 decision #8, upstream V1's rule). `ProviderAdapter.ts` adds `ProviderResumeFailedError` and an optional `nativeThreadMissing` on `ProviderAdapterResumeThreadError`; `Adapters/CodexAdapterV2.ts` sets it only for Codex's own `thread/resume` error answer matching V1's `isRecoverableThreadResumeError`, and `Adapters/OpenCodeAdapterV2.ts` only for `isOpenCodeNotFound` (structured 404 / `NotFoundError`). Claude resumes lazily, and ACP/Pi adapters never set it. `ProviderTurnStartService.ts` starts a new native conversation with a full-thread digest only for a missing conversation or upstream's uncertain pending history delivery, and records a visible `system_notice` ("Started a new provider conversation"), a warning log and the diagnostic on the resolved transfer. Every other resume failure is a terminal `ProviderResumeFailedError` through run execution: no replacement thread, no compaction, and no history delivery (a delivery attempt would leave a pending marker that forces replacement on the next attempt). Diagnostics render the cause chain without stack frames, bounded and redacted through `makeProviderFailure`; `ProviderFailure.causeMessage` surfaces `ProviderResumeFailedError` and `CodexCliVersionUnsupportedError` text instead of the generic curated message. `testkit/ProviderSwitch.integration.test.ts` simulates native replacement as a missing conversation. Watch signal: upstream V2 gates fresh-start fallback on a provider-reported missing conversation. Exit: remove when upstream lands the equivalent. Upstream path: the give-back backlog, [Jacksondr5/j5code#276](https://github.com/Jacksondr5/j5code/issues/276), which holds the drafted upstream PR until V2 merges upstream.

- **Committed Stop wins over steers and automatic usage-limit resume.** `Orchestrator.ts` `dispatchMessage` checks for a durable `run_interrupt_request` on the run while holding the thread dispatch lock. It blocks three things: a new steer entering a stopped run; a previously accepted non-delegated steer becoming an automatic follow-up after Stop (A2A messages reach Astra as steers); and an automatic usage-limit continuation (`usageLimitContinuationOfRunId`) resuming a run that was stopped before it failed on the limit, which takes upstream's existing no-op branch. The only upstream-file edits are the three conditions and a one-element addition of `run_interrupt_request` to `readCommandProjection`'s `turnItemTypes`; every other reader of that projection filters turn items by type. Human steers are covered too; an explicit new message can start new work. Tests in `j5/a2a/DeliveryTransport.integration.test.ts`: Stop before admission; Stop after admission but before a normal completion; Stop → usage-limit failure → automatic continuation refused, plus its no-Stop control. The restart-continuation guard was dropped on 2026-09-24 (decision #5; restart continuation is off by default in J5), and the runtime-recovery SQL is upstream's. Watch signal: upstream resolves `TODO(interrupt-hardening)` and honors committed Stop in completed-steer follow-up and usage-limit recovery. Exit: remove once equivalent behavior is proven.
- **MCP idle HTTP connection race**. Files: `apps/server/src/server.ts`, `apps/server/src/mcpHttpConnections.ts`, and its focused test. Upstream's `server.ts` is Node-only; the patch is the single `configureMcpHttpConnections(...)` wrap inside `NodeHttpServer.layer(() => …)`. The Node server disables keep-alive only for `/mcp` responses, avoiding reuse of a connection racing Node's idle timeout. Responses and streams finish normally; MCP session identity is independent of the HTTP connection. Tool POSTs are never automatically retried because a failed response can follow a committed side effect. Nine focused transport tests and live real-provider acceptance cover this seam. Watch signal: the upstream Node/provider transport prevents this idle-reuse failure while preserving streaming, session identity and exactly-once tool effects. Exit: remove the delegate and helper when equivalent transport behavior is verified.
- **SQLite boolean bindings in limit-recovery candidates**. `apps/server/src/orchestration-v2/ProjectionStore.ts` `getLimitRecoveryCandidates` binds `options.autoResume`/`options.snooze` as `1`/`0`, because node:sqlite on Node 24.14 rejects JS booleans ("Provided value cannot be bound to SQLite parameter"); that failed the usage-limit recovery query and upstream's own `ProjectionStore.test.ts`. Watch signal: upstream converts the booleans. Exit: remove the hunk. Upstream path: offered through the give-back backlog, [Jacksondr5/j5code#276](https://github.com/Jacksondr5/j5code/issues/276).
- **Upstream V1 cutover test skipped**. `apps/server/src/orchestration-v2/LegacyV1Cutover.integration.test.ts` is `describe.skip` because it seeds an upstream V1 history with a site-local slot 41, which the case-31 wrapper refuses by design (J5 never shipped V1; J5 and T3 never share databases, decision #1). Exit: none while decision #1 stands. This is policy; it stays recorded here so a rebase does not silently re-enable the test.

- **CI test fixtures reflect runtime contracts**. `apps/server/src/entrypoint.test.ts` expects canonical filesystem paths, including macOS `/private/var`. `apps/mobile/src/features/review/shikiReviewHighlighter.test.ts` fixes the clock during its cold-init comparison so Shiki's 500 ms tokenization budget cannot make runner load change token boundaries; the assertion and runtime budget remain intact. Watch signal: upstream supplies these portable fixtures and deterministic timing. Exit: remove each matching test delta when that correction lands. The Claude composed-prompt assertion remains the separate case 8 contract.

### A2 root-spawn and single-target stop composition

The post-A6 A2 verb slice was rebased after the merged creation and Thread Homes work onto
`j5/main` at `ee87660a175579267425426109e8fb8de634b8dd`, a docs-only descendant that
includes the self-messaging law at `48037fadd6929e8b7f79c697a0031958a9ae416c`. It extends only the J5-owned
MCP toolkit and runtime composition: `spawn_agent` creates an ordinary root-lineage
thread, atomically records immutable home plus placement/provenance facts through
the existing A6 services, and then dispatches one stable first-turn brief;
`stop_agent` resolves exactly one participant with `listParticipants` and calls
the unchanged single-thread interrupt primitive. `PlacementCascadeService` and
`listSubtree` have no A2 consumer.

The authoritative total lifecycle lock order is `drainPermit ≺ appendPermit ≺
mutationPermit ≺ DB`. Spawn never drains: its coordinator never acquires
DeliveryWorker's private `drainPermit`, and a delivery drain may run concurrently
while contending for `appendPermit`. The coordinator starts at `appendPermit`, then
takes the placement `mutationPermit`, then opens one outer SQL transaction. Its
non-reentrant permits are acquired once; the permit-free home/placement primitives
never re-acquire them or start a nested transaction, and the ledger publishes
`participant.joined` only after the outer commit succeeds. If a future lifecycle
path exposes and holds a drain permit while composing, it must acquire that permit
before `appendPermit`. A forced-interleave DeliveryWorker regression is the gate
against restoring the former DB-permit-first lock inversion; rollback must leave
neither durable facts nor a committed-event publication.

Spawn results report the committed A6 provenance source `j5_spawn`; they never
translate it to the stale `j5_wrapper` wording whose Product documentation true-up
is in flight. Models without a reasoning-option descriptor, including Grok and
custom settings models, remain excluded because this contract requires an
explicit reasoning selection. Admitting them requires a future contract amendment,
not a default or no-op selection. Optional client request keys make both lifecycle
tools non-idempotent at the MCP hint level: supplying a stable key still provides
replay, while a previously rejected create permanently binds that key and requires
a fresh one. A post-create fact failure can leave a visible unregistered thread;
A9, not this slice, owns its eventual retirement path.

The self-messaging revision is governed by `48037fadd` and
`docs/j5/worklog/picker-and-self-messaging-rulings-2026-08-31.md`: the existing
caller directory row is marked `self=true` with messaging/exchange capability
disabled, `send_message` rejects the caller's own derived participant id before
storage and names `list_participants` and `schedule_task` as the shipped alternatives,
and the one replay-stable spawn turn carries committed `participant_id` plus
Squadron facts in platform context beside the byte-preserved spawner brief.

For `stop_agent`, Squadron membership is the authorization boundary. Placement
selects exactly one agent/thread; that target thread's committed projection supplies
the project id required by the unchanged interrupt primitive. Caller project scope
is deliberately not used as authority or routing truth.

`t3_thread_organize` replaces `archive_agent`. The J5 handler adds Squadron authorization for archive/unarchive of another registered agent; upstream project access still applies. Closure is performed once by the existing PR132 lifecycle reactor for every command door. The internal `ArchiveAgentService` remains for crew confirmation and interruption; only its MCP tool is retired. Crew-seat archive is refused by organize, and Captain archives retain the existing crew cascade. Ordinary non-crew agent archive adopts upstream self-archive and active-run semantics and has no token exchange. Human UI warnings remain. The obsolete standalone archive MCP handler and tool schemas are removed; archive/unarchive lifecycle, terminal notices and identity retention are covered through the replacement command path.

The composed HTTP registration test pins 72 tool names, rejects direct calls to the retired/raw tools and eight deferred mutators, and keeps the original per-capability credential checks. The original J5 directory, spawn, stop and Exchange contracts remain in force.

These are per-instance Director/Jackson-authorized exceptions, not standing category permission. The earlier fork rebrand is separately complete at `0c0de1acefea00a34f9529bb97be32ff5056cfcc`; its rebase-critical boundary is recorded in `BRANDING.md:1-5,24-35`. The supporting fork setup plan (`artifacts/fork-setup-plan/index.md:8-12`) and its six T1-T6 ticket artifacts are internal project records and are not present in this repository.

If a required change cannot fit this discipline, stop and review the exception before implementing it.

### Saved-agent mentions (2026-09-08, revised 2026-09-14)

Upstream settings sections no longer accept a `description` prop; J5 library panels put explanatory copy in a leading `SettingsRow`.

`apps/server/src/orchestration-v2/ThreadMessageIntake.ts` treats the J5 `resolve-agent-persona` launch refusal as pre-dispatch failure and releases claimed uploads; the pending upload remains available for retry.

Bryant requested a shared agent picker for Codex and Claude, invoking a saved agent as a subagent within the current conversation. This is a narrow exception to the earlier native-only Subagent steering: the child uses the existing delegated-child engine with `lineage.relationshipToParent = subagent`. Its child thread is backing storage, not a top-level Peer Agent. Ordinary native subagent requests and `spawn_agent` retain their existing meanings; `spawn_agent`'s optional `agent` input (2026-09-09) resolves a saved agent into a Peer Agent's immutable assignment entirely inside `apps/server/src/j5/agents/agentPersonaSpawn.ts` and the J5 toolkit handlers, with no further upstream hook. Proposal-driven Crews use ordinary conversation. The former `/crew` send-path wrapper in `ChatView.tsx` and slash-menu spread in `ChatComposer.tsx` are removed; case 8's standing instructions guide the agent to propose a roster, including custom members, and wait for the person's decision before launching. The platform's `<j5_crew_gate>` decisions render as cards through case 24's existing `renderThreadA2ADelivery` delegate: J5-owned `apps/web/src/j5/crew/CrewNoticeRenderer.tsx` and `crewNotices.logic.ts` recognize the tagged blocks strictly (anything unparsed stays raw), and the one upstream call site in `MessagesTimeline.tsx` gains two fields on the same call, `threadRef` and `markdownCwd`, so a seat opens its thread and the brief renders as markdown (2026-09-16). Mobile has no delivery seam and shows these notices as text, as it does A2A deliveries. `ChatView.tsx` mounts J5-owned `CrewRosterGate` (`apps/web/src/j5/crew/CrewRosterGate.tsx`) immediately above `ChatComposer`, outside its form so roster focus does not expand the composer: the initial roster proposal is answered inline above the Captain's composer like a planning question, while mid-run seat additions stay on the inbox bell (`crewProposals.logic.ts` decides which is which). The human gate (open proposals, approve with edited seats, decline) lives entirely in J5 files: `apps/web/src/j5/crew/`, the J5 inbox page and bell, and server routes under `apps/server/src/j5/a2a/CrewProposalsHttp.ts`. One adapter append makes the gate reachable from Claude: `claudeMcpQueryOverrides` in `apps/server/src/orchestration-v2/Adapters/ClaudeAdapterV2.ts` spreads J5-owned `J5_CLAUDE_MCP_ALLOWED_TOOLS` (`apps/server/src/j5/a2a/mcp/claudeAllowedTools.ts`) into the read-only pre-approval list, because Claude runs read-only personas in `dontAsk` mode and would otherwise deny every J5 coordination tool; the upstream `CLAUDE_READ_ONLY_T3_MCP_ALLOWED_TOOLS` constant and its toolkit cross-check stay untouched, and the two exact-allowlist assertions in `ClaudeAdapterV2.test.ts` include the J5 spread. The Codex counterpart is one spread in `codexThreadRuntimeParams` (`apps/server/src/orchestration-v2/Adapters/CodexAdapterV2.ts`): the injected `mcp_servers."t3-code"` entry gains J5-owned `J5_CODEX_T3_MCP_SERVER_CONFIG` (`apps/server/src/j5/a2a/mcp/codexToolApproval.ts`; since 2026-09-14 a per-tool `tools.<verb>.approval_mode = "approve"` map naming the J5 verbs plus `write_artifact` (a saved agent's declared handoff artifact lives in application storage, never the workspace) and, since 2026-09-15, the provider-native Subagent verbs `delegate_task`, `task_status`, and `task_cancel` (a Crew member refused `spawn_agent` is sent there), never a server-wide default, so worktree handoff, browser preview, and scheduling keep Codex's own verdict for read-only personas), because Codex 0.153+ rejects every non-read-only MCP tool under approval policy `never` with "MCP tool call requires approval, but approval policy is never"; that policy is what full-access mode and every saved-agent runtime policy send, so without the spread `send_message`, `spawn_agent`, and `propose_crew` all fail on Codex. The full spread applies when the thread's resolved approval policy is `never` (`codexApprovalPolicyIsNever`). Interactive modes pre-approve only routine messaging and the crew proposal/request tools; membership still waits for the platform's human roster or Inbox decision. Other tools retain Codex's prompting. The `codexThreadRuntimeParams` assertion in `CodexAdapterV2.test.ts` includes the field.

`packages/client-runtime/src/state/models.ts` accepts a status-only runtime in `threadRuntimeIsActive`; J5 crew state reuses that exported predicate without importing the upstream-private status helper. Preserve this narrow input contract when carrying crew reads forward.

The proposal flow's server modules are J5-owned and need no upstream registration: `AgentCrewInstanceService`, `AgentCrewProposalService`, `CrewLaunchService`, `CrewProposalService`, `CrewLaunchReporter`, `crewGateNotice`, `crewFailureAlert`, `CrewSeatFinishNotifier`, and `CrewProposalsHttp` under `apps/server/src/j5/a2a/`, the `list_personas`, `propose_crew`, `request_crew_member`, and `archive_crew` verbs in the J5 toolkit, and J5 migrations 14 (`j5_agent_crew_instance`, `j5_agent_crew_member`, `j5_agent_crew_proposal`) and 16 (proposal claims and launch-report receipts), plus 15 (`agent_id` nullable on `j5_agent_crew_member`: a custom seat names no saved agent and defaults to the Captain's runtime, with explicit harness/model/options/access selections stored in the proposal and bound to human approval). `archive_crew` composes the J5 `ArchiveAgentService` (which gained a side-effect-free `readFacts` and a `confirmationSatisfied` input for unit archives), and `archive_agent` refuses Crew members. Names are one line each and seat names are lowercase-hyphen words (`crewLimits.ts`), enforced by the MCP schemas and again in `CrewProposalService.validateSeats` for the human's edited card, because the platform writes them into the gate and seat notices ahead of the fields the Captain's card parser trusts. A roster launch records its Crew (every planned seat under deterministic ids) before any seat thread exists, so a spawn that fails partway leaves nothing a Crew record does not know: the gate is handed back with the human's approved seats kept, a retry converges on the same seats (a renamed seat's stale reservation is dropped, and a seat whose brief already dispatched is compared on the Captain's brief and its own instructions, not on the roster block, so dropping a failed seat does not refuse the rest), and a decline retires the record and archives whatever seat threads the failed launch created (2026-09-17, after Jackson's review of PR #148). Crew handoff artifacts (2026-09-10, consolidated 2026-09-14) add no path of their own: a seat whose definition declares an `outputArtifact` writes the same shared handoff artifact every saved agent writes (`handoffs/<agent>/<Artifact>-<task>.md`, PR #142), the handoff gate in `apps/server/src/j5/agents/agentHandoffObserver.ts` checks for it and reminds the seat once, and J5-owned `CrewSeatFinishNotifier.ts` tells the Captain in its thread when the seat finishes and whether the file exists; it settles no thread (2026-09-17, Jackson's review: upstream's settle rules apply to seats). That notice is keyed by the seat's participant (one Captain's several Crews reuse seat names), carries the handoff's size and digest so a rewritten body too long to inline still reads as changed, escapes the opening seat tag as well as the closing body tag in quoted handoffs, and a failed notice is left for the next pass rather than swallowed (2026-09-17, after Jackson's review of PR #150). The earlier `deliver_artifact` verb, `j5_agent_artifact` table, and crew-only read verbs were removed before shipping. Two leaf allowlists carry `write_artifact` for read-only personas so a seat can write its handoff artifact into application storage: `J5_CLAUDE_MCP_ALLOWED_TOOLS` (spread into Claude's read-only pre-approval list) and `J5_CODEX_PREAPPROVED_TOOLS` (per-tool `approval_mode` entries on the `t3-code` server for Codex under approval policy `never`). The crew gate rides the J5 client-runtime surface (`packages/client-runtime/src/j5/http.ts`, `state.ts`; contracts in `packages/contracts/src/j5.ts`), so proposals are a per-environment read source like the inbox. Crew launch remains out of scope for `delegate_task`.

Following Jackson's 2026-09-14 review, the J5-only `invoke_agent` tool is gone. J5 registers its own `delegate_task` (`apps/server/src/j5/agents/agentDelegation.ts`): upstream's input plus an optional `persona` id, with a J5-written description that leads with `persona` and presents the plain child as the fallback for cross-provider or T3-tracked work (upstream's text tells the model to delegate for any subagent request, which would contradict the native-subagent steering). With `agent` it resolves the saved definition, route, and permissions in the J5 invocation module and passes the pinned assignment to `OrchestratorMcpService.delegateTask`; without it, it is upstream's plain child delegation. `task_status` and `task_cancel` are consumed from upstream unchanged so a parent can follow the child. Raw thread creation (`create_threads`, `t3_thread_start`, `t3_thread_send`, `t3_thread_interrupt`) stays excluded.

Keep implementation under `apps/{server,web,mobile}/src/j5/agents`, `packages/client-runtime/src/j5/agentMentions.ts`, and `packages/shared/src/j5/agentMention.ts`. Saved definitions, route resolution, permission checks, and immutable snapshots stay in the J5 invocation module. The upstream engine still owns persistence, child lifecycle, completion delivery, and cancellation.

| Upstream hook                                                                                                                                                              | Keep during an upstream update                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Web `ChatComposer` / `ComposerCommandMenu`; mobile `use-composer-command-menu` / `ComposerCommandPopover`                                                                  | Call the J5 picker hook for the `@persona:` trigger and, since Jackson's 2026-09-14 review, also for bare `@` path triggers, where the hook returns only prefix matches on persona id or name and the composer spreads them ahead of upstream's thread references (#12296) and the file results (order: saved agents, threads, files; one appended spread per client); append the persona item variant, and insert `@persona:<id>`. Path results stay agent-free; reuse existing menu keyboard, selection, and dismissal behavior. Do not fork either composer or native editor.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Web `composer-logic`; shared `composerTrigger` / `composerInlineTokens`; shared/client-runtime package exports                                                             | Append the agent trigger through the J5 detector, after upstream's `#` pull-request and skill detection and before bare-`@` path detection; `ComposerTriggerKind` is the union of J5 `"agent"` and upstream's kinds, including `"pull-request"`. Exclude unquoted agent tokens from file chips. Keep file, pull-request, skill, slash-command, and quoted-path behavior intact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `packages/contracts/src/orchestrationV2.ts`                                                                                                                                | Optional assignment on `delegated_task.request`; public commands must reject server-resolved assignments on both direct and delegated creation. No new public launch RPC or migration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OrchestratorMcpService.delegateTask`                                                                                                                                      | Internal optional assignment pass-through only; the public generic delegate-tool input remains unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `Orchestrator.dispatchDelegatedTaskRequest`                                                                                                                                | Call the J5 snapshot validator and attach the explicit assignment to the child. Do not inherit a parent's persona for ordinary/native children or refactor adjacent orchestration logic.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `T3OrchestrationInstructions`                                                                                                                                              | Append J5 mention instructions for both providers. Explicit persona mentions call `delegate_task` with `persona`; other subagent requests keep native steering.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Existing MCP registration tests                                                                                                                                            | Include `delegate_task`, `task_status`, and `task_cancel` in exact production tool inventories while retaining exclusions for raw thread creation and peer-management alternatives.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Web `ChatComposer` / `ChatView`; mobile `NewTaskDraftScreen` / `new-task-flow-provider` / `thread-outbox-model` / `use-thread-outbox-drain` / `lib/projectThreadStartTurn` | Draft-as-agent entry point (2026-09-10). The composer calls the J5 `useDraftAgentAssignment` hook, mounts `AgentDraftPicker` beside the model picker for local drafts, and renders the J5 lock control from the draft preview with a clear action; `ChatView` spreads `draftAgentPersonaLaunch(routeThreadKey)` into `createThread` and clears the draft after a successful first send. Mobile snapshots optional `creation.agentPersonaId` in the persistent thread outbox and carries it into the launch spec's `agentPersona` during drain. Editing or restoring a rejected queued task re-seeds the draft choice. Unqueued choices remain session-local in J5 `agentDraftState.ts`; existing queued records without a persona still decode.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Web lineage rows / subagent cards / `Sidebar`; mobile `thread-list-v2-items`; `packages/client-runtime/src/state/subagentRuntime.ts`                                       | Agent identity (2026-09-10; AgentsPanel deleted upstream in #12835). `RuntimeSubagent` gains `childThreadId` from the projection. Upstream lineage rows (`chat/ThreadRelationshipsControl.tsx`, non-parent rows) and subagent cards (`chat/V2LifecycleRow.tsx` `SubagentTimelineLink`) mount J5 `AgentRowIdentity` (persona chip with route/policy tooltip + saved-agent handoff link/status) as a sibling of upstream's row button, never inside it (no nested interactive elements); the subagent card gains one flex wrapper for this, and each Lineage row wraps upstream's full-width row control in one shrinkable `flex min-w-0 flex-1` slot so the title truncates and the chips stay inside the panel. The sidebar passes the shell's `agentPersonaAssignment` to the J5 `ThreadCardIdentity`, and the mobile row mounts the J5 `AgentIdentityChip`. All rendering lives in `src/j5/agents/AgentIdentityChip.tsx` on each client.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `apps/server/src/server.ts` runtime composition; `j5/a2a/runtimeLayer.ts`; web `ChatComposer`; mobile `ThreadComposer`                                                     | Handoff artifacts (2026-09-14, Jackson's #124 review). `server.ts` wraps the J5 artifact run-finalization observer in the J5 handoff observer (`j5/agents/agentHandoffObserver.ts`) and provides the shared nudge queue layer; the J5 auxiliary layer merges the nudge worker, which needs `ThreadManagementService`. The composers pass `threadId` into the existing J5 persona lock control so it can show the handoff chip. Handoff state lives in `j5_agent_handoffs` (J5 migration 12); no upstream table, command, or event changes. Two adapter appends make `write_artifact` callable without a prompt for every read-only Claude thread and every Codex thread under approval policy `never`, saved agent or not (artifacts live in application storage, so neither widens the workspace sandbox): `claudeMcpQueryOverrides` in `Adapters/ClaudeAdapterV2.ts` spreads J5-owned `J5_CLAUDE_MCP_ALLOWED_TOOLS` (`j5/a2a/mcp/claudeAllowedTools.ts`) into the read-only allowlist because `dontAsk` denies anything not pre-approved, and `codexThreadRuntimeParams` in `Adapters/CodexAdapterV2.ts` spreads J5-owned `j5CodexT3McpServerConfig(runtimePolicy)` (`j5/a2a/mcp/codexToolApproval.ts`) into the injected `mcp_servers["t3-code"]` entry because Codex rejects non-read-only MCP tools under approval policy `never`; both lists are J5 leaf modules, the upstream constants and the read-only toolkit cross-check are untouched, and the exact-allowlist assertions in both adapter tests include the spreads. `ws.ts` passes `observeRpcStream` into the J5 handler factory (one appended property) and hands each session layer the `AgentHandoffRefreshes` instance it resolves at route-layer build (`Layer.succeed`, exactly like `PullRequestService`); the J5 auxiliary layer exports that J5 layer to the routes, and `server.ts` provides the same layer object to the observer, which bumps it after every row write so `subscribeHandoffRefreshes` lets every chip share one environment-wide handoff query; `packages/client-runtime/src/rpc/client.ts` appends that tag to `EnvironmentSubscriptionRpcTag`. Versioned handoff artifacts (2026-09-15, split out at Tyler's review): `ArtifactMcpService.write` routes `handoffs/` paths to the J5 `ArtifactWorkspace.writeVersioned` (one appended branch), and the `write_artifact` description in `toolkits/artifacts/tools.ts` gains one sentence saying handoff paths add a version instead of replacing; the J5 artifact bullet in `T3OrchestrationInstructions` says the same. The format, parser, and size cap live in `j5/artifacts/ArtifactWorkspace.ts`. On rebase, keep the observer nesting order (handoff → artifact → upstream) and the same queue and refresh layer instances on both sides. |

Rebase checks: run J5 agent mention/invocation tests, exact MCP registration tests, and the existing composer token tests. Verify immutable child instructions/model/policy, blocked/disabled agent rejection, parent permission limits, and identical syntax across web/desktop/mobile. Provider adapters, native SDK configuration, upstream lifecycle machinery, and database migrations need no edits for the mention feature itself; the handoff row above lists the two adapter appends and J5 migration 12 that the handoff work added later.

### PR #75–#86: Role library and runtime-agent presentation

Revision scope approved by Bryant on 2026-09-08 following Jacksondr5's PR #75 review. The stack baseline is `37ef461f704f52d9af2ad776e4ec0bd6b3db09d4`; its pre-revision tip is `bc55a9ce67bdeee0b3b3f8f71885c5ee86aee7a8`. These cases cover this stack's changes to the T3-owned surfaces, including tests and new cross-surface modules. They are scoped integration exceptions, not permission to refactor adjacent upstream code. The older A2A inventory above remains historical to its own pin.

| Integration point                                                                                                                                                                                                                                                                         | Purpose and ownership                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Rebase / validation boundary                                                                                                                                                                                                                                                                                                                                                                            |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/contracts/src/j5/agentPersona.ts`, `j5/agentPersona.test.ts`, `orchestrationV2.ts`, `index.ts`                                                                                                                                                                                  | Persona vocabulary, catalog, import, and library RPC schemas live in the J5 module and reach consumers through one appended barrel export; `orchestrationV2.ts` keeps only additive-optional `agentPersonaAssignment` fields (thread, shell, `thread.create`) and the optional `agentPersona` launch input, plus the `OrchestrationV2PublicCommand` filter that rejects client-supplied assignments.                                                                                                                                           | Preserve optional old-event decoding, custom ids, and digest validation. No J5 loader or runtime behavior belongs in shared contracts; new persona schemas go in the J5 module, never in `orchestrationV2.ts`. Persona contract cases live in `packages/contracts/src/j5/agentPersona.test.ts`; `orchestrationV2.test.ts` is unchanged.                                                                 |
| `packages/contracts/src/rpc.ts`, `apps/server/src/auth/RpcAuthorization.ts`, `apps/server/src/ws.ts`                                                                                                                                                                                      | `WsRpcGroup` gains `.merge(J5AgentPersonaRpcGroup)` and, since case 33, `.merge(J5ArtifactRpcGroup)`; `RPC_REQUIRED_SCOPES` spreads `AGENT_PERSONA_RPC_SCOPES` and `ARTIFACT_RPC_SCOPES`; the handler object spreads `makeAgentPersonaRpcHandlers(...)` and `makeArtifactRpcHandlers(...)`. Definitions, scopes, and handlers live in `packages/contracts/src/j5/agentPersona.ts` and `apps/server/src/j5/agents/agentPersonaRpc.ts`.                                                                                                          | Keep exactly these three one-line appends. The J5 methods use the `j5.agentPersonas.*` tag namespace; the upstream `satisfies` coverage check still applies to them. Scope coverage lives in `j5/agents/agentPersonaRpc.test.ts`; `RpcAuthorization.test.ts` is unchanged.                                                                                                                              |
| `apps/server/src/orchestration-v2/ThreadLaunchService.ts`                                                                                                                                                                                                                                 | Optional persona launch input; one `resolveAgentPersonaLaunch(...)` call before the durable create and one `durableLaunchModelSelection(...)` read after it, both from `j5/agents/agentPersonaOrchestration.ts`. The ordinary launch receipt, provisioning, and durable command remain upstream-owned.                                                                                                                                                                                                                                         | Keep J5 definition discovery, validation, routing, and snapshots outside this service. Receipt replay must reuse its assignment and route, not reload changed source. Focused launch tests cover persona and ordinary launches. Persona launch cases live in `j5/agents/agentPersonaLaunch.integration.test.ts`, which mirrors the upstream launch harness; `ThreadLaunchService.test.ts` is unchanged. |
| `apps/server/src/orchestration-v2/Orchestrator.ts`                                                                                                                                                                                                                                        | `dispatchThreadCreate` calls `guardAgentPersonaThreadCreate(...)`; the thread-command and message-dispatch paths call `agentPersonaRouteLockedError(...)` / `agentPersonaModelMismatchError(...)`; the assignment is spread onto the created thread. All logic lives in `j5/agents/agentPersonaOrchestration.ts`.                                                                                                                                                                                                                              | Retain existing event and receipt mechanisms. Validate against immutable snapshot content, not the mutable source library. Do not reinline the guards.                                                                                                                                                                                                                                                  |
| `apps/server/src/orchestration-v2/ProjectionStore.ts`, `SubagentProjection.ts`                                                                                                                                                                                                            | Retain optional assignment metadata in thread/shell projections; provider-native children do not inherit application persona identity.                                                                                                                                                                                                                                                                                                                                                                                                         | Rebuild must preserve assignment references without reading mutable files. No new upstream migration ids or alternate projection store. Rebuild coverage lives in `j5/agents/agentPersonaProjection.test.ts`; `FoundationPersistence.test.ts` is unchanged.                                                                                                                                             |
| `apps/server/src/orchestration-v2/RuntimePolicy.ts`, `ProviderAdapter.ts`                                                                                                                                                                                                                 | Pass J5-owned policy/instruction composition through the existing runtime-policy seam. `j5/agents/agentPersonaRuntime.ts` owns the persona behavior.                                                                                                                                                                                                                                                                                                                                                                                           | Preserve ordinary runtime modes, project/worktree cwd resolution, legacy assignment compatibility, and failure on unavailable snapshots. Do not inline persona-specific content or policy logic here. Persona policy cases live in `j5/agents/agentPersonaRuntimePolicy.test.ts`; `RuntimePolicy.test.ts` is unchanged.                                                                                 |
| `apps/server/src/orchestration-v2/Adapters/CodexAdapterV2.ts`, `ClaudeAdapterV2.ts`                                                                                                                                                                                                       | Add versioned persona instructions to native developer/system instruction channels and consume the canonical policy.                                                                                                                                                                                                                                                                                                                                                                                                                           | Keep existing provider prompts and provider-native session semantics. No embedded example prompts, folder reads, unrestricted Publisher mapping, or new workflow engine in adapters. Instruction-channel cases live in `j5/agents/agentPersonaAdapterInstructions.test.ts`; the adapter test files are unchanged.                                                                                       |
| `packages/client-runtime/src/j5/agentPersonas.ts`, `j5/agentPersonaAtoms.ts`, their tests, `package.json`                                                                                                                                                                                 | Shared presentation/import helpers and the environment-scoped RPC atoms for the persona library; `package.json` appends the `./j5/agent-personas` and `./j5/agent-persona-atoms` export entries. Web and mobile each instantiate the atoms in `src/j5/agents/agentPersonaAtoms.ts`.                                                                                                                                                                                                                                                            | Web and mobile display the same full server response, including custom ids and display names; clients do not resolve routes or load server files. `state/orchestration.ts` carries no persona atoms.                                                                                                                                                                                                    |
| `packages/client-runtime/src/operations/commands.ts`, `.test.ts`, `state/models.ts`, `state/entities.test.ts`                                                                                                                                                                             | Carry the optional persona launch request and the shell assignment through existing commands/state.                                                                                                                                                                                                                                                                                                                                                                                                                                            | Preserve backward-compatible ordinary tasks and environment-qualified queries.                                                                                                                                                                                                                                                                                                                          |
| `apps/web/src/routes/settings.personas.tsx` (was `settings.agents.tsx` until the 2026-09-17 persona rename; the route is `/settings/personas` and the search entry keeps "agents" as a term), `components/settings/AgentSettingsPanel.tsx`, `SettingsSidebarNav.tsx`, `settingsSearch.ts` | Settings navigation/search registrations and the added environment-aware library view.                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Keep the panel a thin mount of J5-owned `j5/agents/AgentLibrarySettings.tsx`, which owns folder/file picking and library actions. No editor or composer activation is introduced. Catalog error, empty, blocked, and available states must remain visible. Desktop inherits this web surface. The search-entry test lives in `apps/web/src/j5/agents/settingsSearch.test.ts`.                           |
| `apps/mobile/src/Stack.tsx`, `features/settings/SettingsRouteScreen.tsx`, `SettingsAgentsRouteScreen.tsx`, `components/settings-sheet-targets.ts`                                                                                                                                         | Mount J5-owned `j5/agents/AgentLibrarySettingsScreen.tsx` on iOS and Android at `/settings/personas`; `SettingsRouteScreen.tsx` appends one Personas row to upstream's Automations section; `pickAgentDefinitions.ts` handles native folder/file access and foreground handoff. `SettingsSection.tsx` is unchanged; the library screen renders its own header row.                                                                                                                                                                             | Preserve existing navigation and selected-environment routing; no separate persona catalog or policy logic.                                                                                                                                                                                                                                                                                             |
| `apps/web/src/components/ChatView.tsx`, `components/chat/ChatComposer.tsx`, `apps/mobile/src/features/threads/ThreadComposer.tsx`                                                                                                                                                         | Pass the durable assignment to the composers; each composer renders the J5-owned `AgentPersonaAssignmentControl(s)` from `src/j5/agents/` in place of its model controls when an assignment exists. The web composer's `selectedModelSelection` also passes through J5-owned `composerModelSelectionForThread` (`src/j5/agents/personaComposerSelection.ts`, 2026-09-10) so a persona thread always sends its launch route: without it a remembered draft pick made every human message to a Captain fail with "has an immutable model route". | Ordinary tasks retain existing controls. Labels use the launch snapshot, never today's definition name; server enforcement remains authoritative. Keep the composer edit to the prop and the one conditional render.                                                                                                                                                                                    |
| `packages/client-runtime/src/state/subagentRuntime.ts`, `.test.ts`, `components/chat/ThreadRelationshipsControl.tsx`, `components/chat/V2LifecycleRow.tsx`, `MessagesTimeline.test.tsx`                                                                                                   | Stack #84/#86 normalizes runtime-child labels separately from reusable Role definitions. Upstream deleted `AgentsPanel` (#12835); the concern now lives in lineage rows and subagent cards (`AgentRowIdentity`, mounted beside the row button).                                                                                                                                                                                                                                                                                                | Keep this concern separate from catalog loading. Preserve provider-native activity semantics, focused label/panel tests, and no continuous animation.                                                                                                                                                                                                                                                   |
| `apps/web/src/routeTree.gen.ts`, `pnpm-lock.yaml`                                                                                                                                                                                                                                         | Generated route registration and dependency resolution carried by the original stack.                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Regenerate only with the normal tooling when inputs change. The library revision adds no dependency and does not hand-edit either file.                                                                                                                                                                                                                                                                 |
| `docs/README.md`, `docs/user/personas.md`                                                                                                                                                                                                                                                 | Shipped-product documentation and navigation for the runtime panel and persona library.                                                                                                                                                                                                                                                                                                                                                                                                                                                        | User docs describe product behavior; source paths and operational configuration belong in `docs/operations/persona-library.md`.                                                                                                                                                                                                                                                                         |

All persona definitions, source discovery, snapshot storage, routing, authority vocabulary, and prompt composition live under `apps/server/src/j5/agents`. Snapshot files share the environment's existing state lifecycle and must travel with database backups; they introduce no upstream migration or live-install writes during development. Runtime instructions remain outside shell and catalog payloads to keep list rendering and remote synchronization small.

The existing runtime-panel polish is a separate stack concern and is not rewritten by the folder-library fix. The integration review reduced the launch hook to a J5 helper call and kept the runtime-policy seam generic. Broader upstream refactors remain outside this exception. Focused tests and scoped typechecks are the verification boundary; browser/simulator validation is a separate user-authorized step.

### Fork-owned migration lane

J5 schema migrations use a J5-owned migrator, tracking table, and migration-id space. Never register
a J5 migration in upstream's `apps/server/src/persistence/Migrations.ts`: after an upstream advance,
a previously recorded J5 id could cause a new upstream migration with the same or lower id to be
silently skipped. Keep the lanes independent and retain only the small startup call that runs J5
migrations after upstream migrations.

35. Issue #74's machine-sender surface (Jackson's 2026-09-15 ruling: a first-class scope rather than a scope-less token convention). `packages/contracts/src/auth.ts` appends the `a2a:send` literal as `AuthA2ASendScope` to `AuthEnvironmentScope`, deliberately outside both `AuthStandardClientScopes` and `AuthAdministrativeScopes`, so no pairing link and no administrative session carries it; only `j5 a2a token issue` mints it, bound by the session subject to one `machine:<name>` participant, and the machine routes authorize on that scope plus a registered subject. `apps/server/src/binCli.ts` appends `a2aCommand` from J5-owned `apps/server/src/j5/cli/a2a.ts` to the subcommand list. Everything else is add-beside: the `machine` participant kind in `apps/server/src/j5/a2a/contracts.ts`, migration 13, `MachineParticipantService.ts`, `RosterService.ts`, and `MachineSenderHttp.ts` entering through the case 9 aggregate; the machine envelope template and the web card's automation tag live in J5-owned files. On every rebase, verify the literal is still outside both bundles, `PAIRING_SCOPE_OPTIONS` in `ConnectionsSettings.tsx` still omits it, and the `a2aCommand` append survives.

36. Provider event log file names: `apps/server/src/provider/Layers/EventNdjsonLogger.ts` `resolveThreadSegment` ends the segment with a twelve-character hash of the whole thread id whenever normalization shortened the id (a colon, a run of punctuation, or the eighty-character cap), because platform-spawned threads (Crew seats, Peer Agents) carry deterministic ids identical up to the cut and every seat of a Crew was landing in one log file. Only a bare uuid id, which normalization leaves untouched, keeps its plain name; ids with colons that were under the cap change name once on upgrade. One appended branch; `EventNdjsonLogger.test.ts` covers it.
37. The lone-seat archive guard (2026-09-17, Bryant's choice of the server dispatch handler over a client-side mirror or an after-the-fact reversal, after Jackson's review of PR #151): `apps/server/src/ws.ts` yields J5-owned `makeCrewSeatArchiveGuard` (`apps/server/src/j5/a2a/crewSeatArchiveGuard.ts`) once beside `threadManagement` and calls it as one appended line in the `dispatchCommand` handler before the command is enqueued. Every client door (web sidebar, mobile swipe, any future client) sends `thread.archive` and `thread.delete` through that one handler, so a live Crew's seat is refused there with a typed error whose message reaches the client through the existing dispatch-error detail; `archive_crew` and the Captain cascade archive seats through the lifecycle service and never meet the guard. A Crew store that cannot be read lets the command through, because a refusal rests on a live fact, not an outage. The web keeps its pre-archive toast as the friendlier early message.

38. Fork and merge-back registration: `orchestration-v2/runtimeLayer.ts` decorates the single `threadManagementProvided` instance with J5 `ThreadLineage.ts`. It reaches both client intake and MCP. A registered source's durable fork atomically records home and fork provenance through the existing creation composition; existing placement policy makes the fork a sibling of its source. A source without registration remains native. Replaying the original command repairs post-commit registration without another fork. Merge-back requires matching registered homes or two native threads; upstream retains lineage/transfer checks. Creation-side placement and composition avoid a delivery-to-thread-management dependency cycle. J5 re-declares fork, merge-back and organize tool schemas from upstream to expose actionable registration errors and enforce Squadron archive authority. Organize also applies the shared crew-seat guard before dispatch; unit retirement continues through the internal archive service. All other adopted tool handlers use original upstream tool objects and handler layers. Follow-ups #133–#136 cover deferred spawn enhancements and eight agent mutators. Case 34's typed HTTP migration is tracked in #137.

39. First-run import homes: `components/onboarding/WelcomeWizard.tsx` mounts a J5-owned fourth Squadron stage after upstream project selection; `routes/welcome.tsx` carries its explicit environment/Squadron choice into the landing draft. Project and Squadron creation wait for the final import action. Upstream scanning and native-session import remain unchanged. J5's authenticated assignment route registers only imported conversations in the selected project through the existing join rules, preserving established homes and retirement. Reopening setup can give earlier unhomed imports their original home; ordinary native threads are untouched. Keep selections across Back navigation, report partial outcomes, and never reconcile an uncertain Squadron creation by name. Connection/provider setup and remote ownership remain upstream-shaped; display names and CLI guidance use J5 branding.

40. J5 release-archive distribution (Jackson's 2026-09-24 decision #3). J5 adopts upstream's self-contained CLI release archives and stops publishing `@jacksondr5/j5code` to npm (last npm version 0.0.42; first archive release 0.0.43 or later). `packages/shared/src/cliRelease.ts` sets `CLI_RELEASE_REPOSITORY = "Jacksondr5/j5code"`, so `j5 update`, `j5 service install`, server self-update, mobile update checks, and desktop SSH remote launch all download `t3-<version>-<platform>.tar.gz` + `SHA256SUMS` from J5's GitHub Releases. The archive's executable keeps upstream's internal `t3` name; `scripts/install.sh` (hosted as a release asset, `https://github.com/Jacksondr5/j5code/releases/latest/download/install.sh`) installs into `$J5CODE_HOME` (default `~/.j5code`, never `T3CODE_HOME`) and links `~/.local/bin/j5`; it accepts only darwin-arm64 and linux-x64. `CLI_ARCHIVE_PLATFORM_KEYS` keeps upstream's five keys, so other hosts get "No release archive was published". `scripts/install.ps1` is repointed at J5 (`j5.cmd`) though no Windows archive ships; `apps/server/src/cli/update.ts` looks for `j5.cmd`. `packages/ssh/src/tunnel.ts` unpacks remote runtimes into `$HOME/.j5code/runtime/versions` (transport bookkeeping stays `~/.t3/ssh-launch`, case 15). `.github/workflows/j5-release.yml` builds, signs (Developer ID + hardened runtime), notarizes and smoke-tests darwin-arm64 and builds linux-x64 archives with upstream's `scripts/build-cli-archive.ts` / `smoke-cli-archive.ts`, and publishes them with SHA256SUMS, install.sh, and the signed desktop build from a `j5-macos-build.yml` run. User strings in `cli/update.ts`, `uninstall.ts`, `updateProgress.ts` and `service.ts` say `j5`/J5 Code. Migration guide (decision #3b): `docs/user/migrating-to-release-archives.md`. Proofs: `packages/shared/src/cliRelease.test.ts`, `scripts/install.test.ts` (J5 home, `bin/j5`, ambient `T3CODE_HOME` untouched), `packages/ssh/src/tunnel.test.ts` (J5 release URL and runtime dir). `J5 Release` refuses versions below 0.0.43 in its resolve job (the npm era owns v0.0.38–v0.0.42 tags; an older archive would be offered as a downgrade), tolerates an `@ref` suffix on the selected build run's workflow path, and runs `scripts/apply-web-brand-assets.ts production apps/server/dist/client` before `build-exe`, because upstream's `cli.ts build` stamps development icons into `dist/client` and the archive serves that directory (F's npm publish did the same swap in `preparePublishIcons`). A local linux-x64 dry run on 2026-09-24 (Node 26.8.2 host for `build-exe`) produced `t3-0.0.43-linux-x64.tar.gz` (81.7 MB) that passed `smoke-cli-archive.ts` and installed via `scripts/install.sh` from a local mirror into a scratch HOME (`j5 v0.0.43`).

41. Background-service rename and legacy J5 unit retirement (Jackson's 2026-09-24 decision #3a). `apps/server/src/cloud/bootService.ts` names the service `j5code.service` (systemd) / `codes.jackson.j5code.service` (launchd) and parses `J5CODE_HOME` in `bootServiceBaseDirOf` (restart/uninstall/update home detection). Fork-owned `apps/server/src/cloud/j5/legacyBootService.ts` describes the pre-0.0.43 J5 unit (`t3code.service` / `com.t3tools.t3code.service`): `isLegacyJ5BootServiceUnit` means it names `J5CODE_HOME`, never `T3CODE_HOME`, and `legacyStopMeansNotRunning` sets the strict-stop rule (systemd exit 5 or "not loaded/does not exist/not found"; launchd exit 3/113 or "No such process/Could not find service"); every other stop failure is fatal. bootService's integration seam: `install` (start) writes the new unit and stops the legacy one strictly; on a real stop failure it restores the previous `service-state.json`, leaves the legacy unit untouched and never activates the new one. It then activates the new unit, verifies it is running, and only then removes the legacy file; if the new unit does not run, it deactivates it, restores the legacy unit and its state file, and never restarts the new unit alongside it. `install({start:false})` refuses with `legacy-service-present`; `restart` and `uninstall` also retire the legacy unit; `status` reports `legacy-service-present`. A T3 Code unit at the legacy path is never read as J5's, stopped, or removed. Upstream-file additions, all J5-marked: `BootServiceStep.acceptFailure`; `BootServiceManager.running` and `activateAndVerify`, which after every activation (install, handover, restart) polls `systemctl --user is-active` or `launchctl print … state = running` up to 10 × 500 ms and otherwise fails with `BootServiceNotRunningError`, because a condition-skipped start exits 0; `gatingDropIns`, so install refuses before any change and status reports `service-dropin-conditions` when `j5code.service.d/*.conf` carries `Condition*=`/`Assert*=` (other drop-ins are allowed); and `BootServicePrerequisiteError.paths`. Before any change, `install` also refuses with `foreign-service-present` when a unit already at the new J5 path lacks the renderer's `T3_BOOT_SERVICE_UNIT` marker (`isRenderedJ5BootServiceUnit`), so a hand-written unit such as an old checkout-based `j5code.service` is left for the owner. `apps/server/src/cli/update.ts` treats a server in the `j5code.service` or legacy `t3code.service` cgroup, or under either launcher, as service-supervised (`isBootServiceCgroup`, `isBootServiceLauncherCommand`), and prints the `j5 service install` hint when the legacy unit is present. Linux drop-ins under `t3code.service.d/` are not migrated automatically; `docs/user/migrating-to-release-archives.md` copies them with `cp -n`, records what it copied for a precise rollback, and stops for the owner on a gating `j5code.service.d` drop-in. Proofs: `bootService.test.ts` "legacy J5 service handover" suite plus the install-suite verify, drop-in and "refuses to take over a j5code.service that J5 did not write" tests (temp HOME, fake ProcessRunner, TestClock), and `cli/update.test.ts` "recognises the J5 service's own server". On every advance check that no upstream change reintroduces `t3code.service`/`com.t3tools.t3code.service` as J5's own name, and that upstream activation paths still go through `activateAndVerify`.

42. Skills catalog: `packages/contracts/src/{rpc,index}.ts` merge/export the J5 catalog schemas; `settings.ts` appends the environment-local, initially empty source and rejects credential-bearing patches; `serverSettings.ts` hides unsafe legacy sources from clients; `server.ts` adds discovery errors separately from provider health. `auth/RpcAuthorization.ts` and `ws.ts` append the J5 RPC scope map and handlers. Explicit refresh delegates to `j5/skills/skillProviderRefresh.ts` (upstream no longer refreshes providers per connection, so J5 adds no connection refresh); the registry keeps machine refresh upstream-shaped and delegates workspace replacement, failure retention and pending-request supersession to `skillWorkspaceRefresh.ts`. Its service exposes forced workspace refresh and pending cwd reads. `ClaudeSkills.ts` exports the config-root resolver and uses the configured instance environment's home. Web Settings adds one Skills navigation/search/route entry; `state/query.ts` exposes the typed failure alongside its display message. `commandPaletteBus.ts` carries an optional `sourcePicker` (environment and callback), and `CommandPalette.tsx` reuses folder/Git browsing without creating a project for that carrier. On rebase, verify source selection/cancellation, unchanged normal Add Project and SQ1 callbacks, environment-scoped authorization, no implicit catalog access, interruptible connection probes, and late workspace responses never replacing newer discovery. Catalog reads and mutations remain in J5-owned modules.

43. Skills inventory extends case 42's single Settings page with Catalog and Installed sections sharing an environment picker. `server.ts` adds optional skill provenance fields. `ClaudeSkills.ts` appends plugin discovery from `j5/skills/claudePluginSkills.ts` and exports its existing settings-path/repository-root helpers; keep its upstream directory-name explanation intact. `ClaudeDriver.ts` passes the instance environment, `ClaudeProvider.ts` appends the bounded bundled-skill reload probe, and `CodexProvider.ts` carries optional provenance. The registry appends J5's cached path resolver at snapshot ingestion and invalidates it on explicit refresh; workspace resolution stays in the J5 refresher. On rebase, preserve plugin enablement/precedence, built-in composer visibility, status-only cache reuse, and environment/project selection. Origin and discovery-state helpers live in `packages/shared/src/j5/skillInventory.ts`; contracts remain wire-only.

44. Skills linking extends the J5 contract/RPC exports and the authorization/handler spreads from case 42 with `j5.skills.links.*`. The Settings inventory mounts J5's link preview, inspected unlink batch, confirmed original-folder deletion, and managed-record panel; search continues to use the single Skills route. The shared J5 inventory helpers classify linkable origins and discovery state; catalog and standalone linking reuse J5 root resolution, filesystem operations, and affected-provider refresh. On rebase, preserve fresh preview validation, canonical project containment, the Git exposure warning, ownership identity and forget-only recovery, stale unlink/deletion preview rejection, and release the mutation permit before refreshing providers. Catalog remains user-scoped; standalone links can target an explicitly selected project.

45. Agent-led playbooks: `apps/server/src/provider/T3OrchestrationInstructions.ts` appends J5-owned retrieval and advancement guidance. All state, YAML reading, MCP operations, and authenticated reads remain under J5-owned modules; advancement never creates turns or controls provider lifecycle. Web `ChatView.tsx` mounts the J5 step board and expands submitted `/playbook` text; `ChatComposer.tsx` and `composer-logic.ts` append an ordinary built-in command. `packages/client-runtime/package.json` exports the shared J5 presentation and expansion helper. `docs/user/playbooks.md` describes that text expansion; upstream's `docs/user/composer.md` is untouched. The runs overview is a section of the existing J5-owned Fleet page. A J5-owned read-scoped revision stream notifies visible clients of committed playbook mutations. The RPC group, authorization scope map, handlers, and subscription tag are appended through existing J5 integration seams; the WebSocket handlers share the server-lifetime PlaybookStore. The J5-owned Settings library mounts within the existing Personas page. Creation uses Playbook Author with explicit Squadron ownership and preserves invested drafts; library reads resolve project/thread identities within the selected environment. Mobile `ThreadDetailScreen.tsx` mounts the native board, `use-composer-command-menu.ts` appends the same text expansion, and `use-thread-composer-state.ts`, `NewTaskDraftScreen.tsx`, and `new-task-flow-provider.tsx` apply it on existing/new-thread submission. The J5-owned mobile library mounts within the existing Personas screen (in upstream's Automations section of mobile Settings) and uses the same Playbook Author launch with an explicit Squadron. Its start-chat action preserves invested drafts. The native board shares the environment revision subscription and only refreshes while focused and foregrounded.

## Pin and upstream advance runbook

Current candidate pin: `67a2be0fdbee7afb64b691f147ed286a108c706b`, from `t3code/codex-turn-mapping` (frozen 2026-09-24). The previous pin was `62aef8587c0263bed5ae2b26c38d6d138a0676f6`; its integration and this advance are recorded below.

The upstream PR branch is moving history and has already been force-rewritten. Do not assume a future branch tip descends from this commit.

Every advance follows [Merging upstream](docs/j5/process/upstream-merge.md) and rewrites the [upstream convergence watchlist](docs/j5/research/upstream-convergence.md).

### Pin log

| Date       | Pin                                        | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------- | ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-15 | `77168d081abbdd7522f90b3b204cc693015d5f26` | Original setup-plan pin. The upstream branch was later force-rewritten; this commit is not an ancestor of the rewritten live tip. No J5 build or baseline was created from it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-08-15 | `993407dd9e57f1edf2f5681d70140bfefeca93cc` | First deliberate reviewed advance, before J5 changes. Review of `038560e58036d51b2576b3c2cd9170a194cefe9e..993407dd9` found 336 commits: the branch is rebased onto upstream `main` at `ad117235b`, with 233 orchestration-branch commits on top. The log retains the V2 runtime, contracts, provider adapters, client cutover, migration renumbering, and follow-up fixes/tests; no commit advertised reverting or retiring V2. Reconciliation commits call out repaired rebase conflicts and restored main features, so T2 must establish a fresh full-suite baseline at this exact pin.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-08-29 | (kept `993407dd9`)                         | Reviewed drift vs live tip `d6ed793b1e445aa81c1c91ff580a1def11db7d0a` (force-rebased again; pin not an ancestor; content diff 988 files). Every inventory anchor byte-identical or trivially additive (runtimeLayer.test.ts +1 distant test block); composed-with V2 surfaces (SubagentProjection, KeyedSerialExecutor, EffectOutbox, orchestrator/worktree toolkits, ThreadLaunchService) byte-identical. Behavioral deltas nearby but non-breaking: McpSessionRegistry capability gating (#7083); 3 new upstream migrations with V2 renumbering 041-049→044-052 (J5's independent migration lane unaffected). Decision: KEEP pin — nothing needed, tip already CONFLICTING with main again, advance would preempt lanes for no A7 cost reduction. #2829 merge proximity judged UNCLEAR: no approval, DIRTY merge state, no maintainer merge-intent statement, round-16 reconciliation treadmill.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-09-02 | (kept `993407dd9`)                         | Adopted upstream content from [`c17d02cff98f2e7b590d4c3a5775d1faa47c2e7b`](https://github.com/pingdotgg/t3code/commit/c17d02cff98f2e7b590d4c3a5775d1faa47c2e7b): Claude Fable 5.1 catalog, aliases, and CLI version gate. This is a constrained cherry-pick, not a new integration case: paths absent in the fork (the upstream adapter and manifest tests plus manifest) were intentionally not introduced; J5's existing legacy classifier keeps both Claude Fable 5 and Claude Fable 5.1 current.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-09-04 | (kept `993407dd9`)                         | J5 classifier edit; upstream's equivalent is `bc03c3640` in a manifest we do not carry; no upstream content adopted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-09-06 | `b9fa1399cfbacf23f35ba9201af8aebe3f41e807` | Reviewed advance from frozen fork `51ee0fd64a2597d6ac4204b2096f164da240dbb3`, preserving both histories with a controlled merge. Carry the migration bridge, combined Codex generator rules and native-resume refusal; adopt upstream Fable 5 legacy classification, human steer default and settlement. Retain active-Astra peer delivery and default-off restart continuation. Initial interactive acceptance covered web; subsequent exact-source CI and desktop packaging evidence are recorded in the final review section below. Native client interaction and packaged-app execution remain unverified.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-09-24 | `67a2be0fdbee7afb64b691f147ed286a108c706b` | Reviewed advance from frozen fork `8f56083a0f9fb6033517657ee308f9263e3a2870`, built by the rewrite runbook below with previous pin `62aef8587c` as content base (132 conflicts). Applied Jackson's 2026-09-24 decisions: migration bridge gains the exact pin arm and retargets the August/September arms (case 31); one-way `statev2.sqlite` cutover (case 1); release archives from `Jacksondr5/j5code` with npm publishing retired, first archive 0.0.43 (case 40); `j5code.service` rename with legacy J5 unit retirement (case 41); desktop `j5code`/`j5code-dev` profiles kept (case 25); native resume narrowed to upstream V1's not-found rule; Committed Stop steer guard kept and restart guard dropped; held queues follow upstream, with J5 never reporting a held delivery as delivered (case 26); header menu, multi-model fan-out, background clone and Undo-archive doors carry Squadron/Crew policy (cases 13, 19, 21); upstream `SidebarThreadHeader` hosts the Squadron scope (cases 9, 16); bare `@` orders saved agents, threads, files; temporary patches retired or kept as listed above; CI guards added to `thread-transfer-report.yml`, `desktop-macos-preview-publish.yml` and `release.yml`'s `resolve_commit`. The final branch `j5/upstream-sync-20260924` blends build A with reviewed ports (C: release version gate, production web icons, `J5_BRANDING.cli.command`, steer-guard projection filter, restyle variants, guide Update-server paragraph; D: usage-limit Stop condition, identity beside row buttons, strict legacy-stop concept, held-delivery test; B: release workflow-path `@ref` tolerance, AGENTS.md snippet), plus new service safety (running check with rollback, gating drop-in refusal); D's persona carrier on multi-model fan-out was rejected because a persona pins one model. Native-client interaction, the packaged app, a real service handover and the release workflow are unverified. |

### Toolchain migration at the first advance

The original setup plan expected Bun. The advanced pin instead declares pnpm `11.10.0`, a `pnpm-lock.yaml`, Node `^24.13.1`, and 15 pnpm `patchedDependencies`. Vite Plus is a pinned repo-local dev dependency; the pnpm prepare step runs its workspace configuration without a separate account or global install.

Builds and CI follow the declarations at the recorded pin: use fnm with the version in `.nvmrc` and run `pnpm install --frozen-lockfile`. Do not create a Bun lockfile or change the machine-wide Node installation to compensate for an old pin's instructions.

The advanced lockfile also selects `sysinfo` `0.39.3`, whose declared minimum is Rust `1.95`.
J5 pins Rust `1.95.0` in `rust-toolchain.toml`; rustup applies that version only inside this
repository, while Jackson's machine default remains independent. The desktop artifact builder
already uses Cargo's committed lock with `--locked`.

Until PR #2829 merges:

1. Keep `j5/main` at the recorded pin between deliberate advances.
2. At most weekly, fetch `upstream` and review the full old-pin-to-candidate diff, including any force-rewrite divergence. Re-check every entry in Temporary in-place patches against that diff and remove the ones whose fix landed.
3. Advance only after that diff and the J5 test baseline are reviewed.
4. Update the SHA and date in this file in the same commit as every advance.

After PR #2829 merges:

1. Review monthly for a deliberate advance onto an upstream release tag, not an unrecorded branch tip. Preserve published `j5/main` ancestry; do not rebase or force-push it.
2. Review upstream release notes and the complete old-base-to-new-tag diff. Re-check every entry in Temporary in-place patches against that diff and remove the ones whose fix landed.
3. Re-run the J5 build, test baseline, and packaging checks before updating `j5/main`.
4. Update the SHA and date in this file in the same commit as every advance.

If upstream squash-merges or rewrites history, freeze the old pin, fork head and selected upstream SHA. Construct a candidate from the new upstream tree, carry reviewed J5 files and minimal delegates, and audit the complete upstream-relative delta. After verification, create a genuine two-parent merge joining the frozen fork head and the tested candidate (which descends from the selected upstream). Its tree must equal the reviewed candidate tree. Preserve milestone refs, obtain terminal CI on the exact published head, and leave final merge and installation to Jackson.

Before every pin advance, run `node scripts/j5/check-upstream-migrations.ts --base <old-pin> --candidate <candidate>`. Renumbering, removed/changed implementations and additions below the recorded high-water mark require explicit mapping and a disposable upgrade proof. `--allow-reviewed-bridge` recognizes only reviewed transitions recorded in the J5 manifests (currently pin `62aef8587c` → upstream `67a2be0fdb`); the candidate must be that upstream commit or descend from it with an identical migration manifest and audited dependencies. It does not approve an arbitrary rewrite or replace database verification. Never insert high J5 IDs into upstream's migration tracker. Snapshot live SQLite read-only with a consistent backup, rehearse on a disposable copy, and retain the baseline for recovery. Since this advance the server database is `userdata/statev2.sqlite`, copied once from `state.sqlite` on first start; the copy is a one-way cutover — stop writers, snapshot first, and never run old and new binaries against the two files.

On every advance, grep the files upstream added since the old pin, not only `BRANDING.md`'s known audit sites, for `t3code`, `T3 Code`, `.t3`, `T3CODE_HOME`, `pingdotgg`, `t3.codes/install` and `npx t3`, and classify each hit against `BRANDING.md` (identity site to rebrand, or deliberately unchanged internal/general copy). Likewise add the `github.repository == 'pingdotgg/t3code'` guard to every new upstream workflow job that would otherwise run or publish from the fork.

## J5 CI and packaging

Fork-owned workflows use the Node version in `.nvmrc`, pnpm `11.10.0`, the frozen pnpm lockfile, and
the repo-local Vite Plus binary. `J5 CI` is the push/PR gate for `j5/**`; `J5 Weekly Full Build` is the
scheduled and manually dispatchable pre-rebase suite plus Apple Silicon desktop build. `J5 Release` publishes CLI archives (darwin-arm64, linux-x64), SHA256SUMS, install.sh and the signed desktop build to GitHub Releases (case 40); npm publishing is retired. The CLI archive job switches the host to Node 26.8.2 only around `build-exe` (Node single-executables need `--build-sea`), runs every repo script through `pnpm exec` so the repo-local `vp` is on PATH, and applies production web icons before packing.

See [`docs/j5/runbooks/macos-packaging.md`](docs/j5/runbooks/macos-packaging.md) for the local build, signature
verification, install, Gatekeeper approval, and workflow runbook.

## Final upstream-file review — 2026-09-06

This inventory describes committed source `f8ba1a653f986835d3e5d0584648cc686f16ceb6`
against selected upstream `b9fa1399cfbacf23f35ba9201af8aebe3f41e807`, compared with
the prior fork delta `993407dd9e57f1edf2f5681d70140bfefeca93cc` →
`51ee0fd64a2597d6ac4204b2096f164da240dbb3`. It records what is present for Jackson's
final carry/drop decision; documentation is not new authorization for a code exception.

There are 177 upstream-owned changed paths (176 modifications and one deletion),
including generated files and fixtures. Of these, 98 preserve the prior patch's added/deleted
line sequence exactly, 59 adapt an existing patch, and 20 have no prior fork delta at that
path. These are patch classifications, not claims that a whole file stayed byte-identical
or that all 20 paths introduce new product policy. Another 295 paths are fork additions.
The table below enumerates every upstream-owned path, including supporting tests.

### New work versus retained fork behavior

This advance introduces three new runtime patch families: the migration-history compatibility
bridge (case 31), committed-Stop guards for upstream's new restart continuation, and the MCP
idle-connection workaround. The other runtime responsibilities in this inventory carry or
adapt earlier J5 changes. In particular, pairing refusal/retry, optional ad-hoc signing, the
0.152.1 Codex protocol pin, the configured 0.151.0 CLI minimum, native-resume refusal and
peer-delivery settlement all predate the frozen fork head. Newly touched upstream paths do
not by themselves indicate new behavior. Build/CI fixes and portable/deterministic test
corrections are additional work from this advance, separate from the three runtime families.

### Verification and review at the recorded source

At `f8ba1a653f986835d3e5d0584648cc686f16ceb6`, [J5 CI push](https://github.com/Jacksondr5/j5code/actions/runs/34064457722),
[J5 CI PR](https://github.com/Jacksondr5/j5code/actions/runs/34064460361), and the
[weekly full test/build/macOS packaging job](https://github.com/Jacksondr5/j5code/actions/runs/34064485547)
passed. The packaging job mounted the Apple Silicon DMG and verified its J5 identity and
strict ad-hoc signature. The [artifact](https://github.com/Jacksondr5/j5code/actions/runs/34064485547/artifacts/9998875188)
is retained until 2026-10-06; the workflow records remain the evidence when that download expires.
Native client interaction, packaged-app execution and second-machine/relay/tunnel acceptance
remain unverified. These results apply to that recorded source, not automatically to a later head.

An independent Codex peer reviewed necessity and fork maintenance at that source and found no
demonstrated runtime defect requiring a pre-merge change. It found documentation omissions and
stale claims, which this documentation revision corrects; its follow-up checked the
substantive corrections. It did not repeat runtime acceptance. The configured CLI support
floor remains unproven at 0.151.0, and documentation does not resolve the remaining maintenance
decisions. Jackson explicitly accepted the restart adaptation with the upstream-first
next-merge instruction in its temporary-patch entry.

### Replacement scope and maintenance obligations

The historical “appended integration cases” label includes substantial instance-level
exceptions: toolkit exclusion and provider steering (cases 2/8), Squadron launch and UI
replacement (cases 9–20/23), scheduled creation refusal (case 12), and native-resume refusal.
These are intentional replacements or behavioral changes, not permission to grow the same
categories. `DraftHeroHeadline.tsx` replaces upstream's full implementation; Sidebar and
CommandPalette replace project-derived selection with Registrar-derived behavior. A future
focused change should consider a J5-owned headline at the existing mount and smaller shared
selection seams. This review authorizes no ownership refactor.

The migration bridge must retain recognition while supported old installations or backups can
still arrive. Retiring that support requires a deliberate support-window/import-tool decision,
not merely an upstream append-only migration. For case 26, retire the core outbox/ended-turn
patches only when upstream supplies equivalent durable outcome waiting and terminal steer
semantics; preserve one shared outbox instance until then.

### Previously under-recorded behavior

- **Pair discovery isolation and activation retry (P).** `apps/server/src/cli/pair.ts`
  refuses ambient/default fallback from a linked worktree that has no running server.
  An explicit `--base-dir` with no runtime file scans at most four times, with three
  one-second activation waits supplied through `RuntimeFileActivationDelay`. A stale
  runtime file does not trigger that wait; missing-file and no-running-server errors
  remain distinct. This is a retained behavioral patch, not merely home renaming.
  `pair.test.ts` covers an already-running explicit-base target, bounded exhaustion,
  stale files and worktree refusal; it does not prove a file appearing during a wait.
  Maintain the refusal for isolation; reassess the extra retry when
  upstream provides a suitable activation contract. Tests inject the delay.
- **Optional ad-hoc macOS signing (S).** `scripts/build-desktop-artifact.ts` adds
  `--adhoc-sign` / `T3CODE_DESKTOP_ADHOC_SIGN`, defaults it off, rejects simultaneous
  certificate signing, and passes `identity: "-", hardenedRuntime: false` for this mode.
  This is a retained packaging exception beyond branding. It enables the personal
  Apple Silicon artifact; it supplies no notarization. `build-desktop-artifact.test.ts`
  covers options, mutual exclusion and configuration. Reassess when upstream exposes
  equivalent personal-build signing; do not carry this as an implicit release default.
- **Fork workflow guards (C).** `.github/workflows/ci.yml` gates each upstream job on
  `github.repository == 'pingdotgg/t3code'`, and `mobile-fingerprint-check.yml` gates its
  fingerprint job likewise. J5's own workflows run on available GitHub-hosted runners.
  This prevents fork jobs waiting for upstream-owned Blacksmith capacity. J5's gate is
  not job-for-job equivalent to upstream's Rust, mobile-native and release-smoke matrix.
  The upstream workflow definitions stay intact behind the guard; compare every new
  upstream job and its coverage at each advance.
- **Test portability and synchronization (T).** The temporary CI-fixture entry also
  includes `ProviderReplayGate.testkit.ts` and its focused test: a deferred arrival
  notification replaces fixed event-loop polling in `OrchestratorScenario.ts` while
  preserving frame hold/release. Canonical-path and explicit-PATH fixtures are runtime
  contract corrections. The Shiki test fixes only the test clock, retaining the actual
  tokenization budget. Remove individual deltas when upstream carries their equivalent.

### Path-to-contract ledger

Contract keys: **B** = branding, app/cloud/update identity in `BRANDING.md`; **H** =
home isolation and canonical live-home guards (cases 15/25); **P/S/C/T** = the behavioral
entries above; **G** = generated Codex protocol, adapter/fixture compatibility and temporary
patches; **N** = native-resume refusal and diagnostics; numbered entries refer to the
stable integration cases above. A path can carry several independent contracts; remove
only the obsolete hunks when upstream supplies one of them. `R` means retained edit,
`A` adapted prior edit, and `N` newly touched upstream path. These history letters do not
indicate approval. The deleted hook remains explicitly marked.

| Upstream-owned path                                                                                        | History | Contract                                  |
| ---------------------------------------------------------------------------------------------------------- | ------- | ----------------------------------------- |
| `.github/workflows/ci.yml`                                                                                 | A       | C                                         |
| `.github/workflows/mobile-fingerprint-check.yml`                                                           | R       | C                                         |
| `.gitignore`                                                                                               | R       | H                                         |
| `apps/desktop/package.json`                                                                                | R       | B                                         |
| `apps/desktop/resources/dmg/dmg-background-latest.svg`                                                     | R       | B                                         |
| `apps/desktop/resources/dmg/dmg-background-nightly.svg`                                                    | R       | B                                         |
| `apps/desktop/scripts/electron-launcher.mjs`                                                               | R       | B, H                                      |
| `apps/desktop/scripts/electron-launcher.test.mjs`                                                          | R       | B, H                                      |
| `apps/desktop/src/app/DesktopAppIdentity.test.ts`                                                          | R       | B                                         |
| `apps/desktop/src/app/DesktopClerk.test.ts`                                                                | R       | B                                         |
| `apps/desktop/src/app/DesktopConfig.ts`                                                                    | R       | B, H                                      |
| `apps/desktop/src/app/DesktopConnectionCatalogStore.test.ts`                                               | R       | B                                         |
| `apps/desktop/src/app/DesktopPreReadyPlatform.test.ts`                                                     | N       | B                                         |
| `apps/desktop/src/app/DesktopEarlyElectronStartup.test.ts`                                                 | R       | B, H                                      |
| `apps/desktop/src/app/DesktopEarlyElectronStartup.ts`                                                      | R       | B, H                                      |
| `apps/desktop/src/app/DesktopEnvironment.test.ts`                                                          | R       | B, H                                      |
| `apps/desktop/src/app/DesktopEnvironment.ts`                                                               | R       | B, H                                      |
| `apps/desktop/src/app/DesktopLinuxUrlHandler.test.ts`                                                      | R       | B                                         |
| `apps/desktop/src/app/DesktopLinuxUrlHandler.ts`                                                           | R       | B                                         |
| `apps/desktop/src/app/DesktopObservability.test.ts`                                                        | R       | B, H                                      |
| `apps/desktop/src/app/DesktopStatePaths.ts`                                                                | R       | B, H                                      |
| `apps/desktop/src/backend/DesktopBackendConfiguration.test.ts`                                             | R       | B, H                                      |
| `apps/desktop/src/backend/DesktopBackendConfiguration.ts`                                                  | R       | B, H                                      |
| `apps/desktop/src/backend/DesktopBackendManager.ts`                                                        | R       | B, H                                      |
| `apps/desktop/src/backend/DesktopServerExposure.test.ts`                                                   | R       | B                                         |
| `apps/desktop/src/electron/ElectronProtocol.ts`                                                            | R       | B                                         |
| `apps/desktop/src/settings/DesktopAppSettings.test.ts`                                                     | R       | B, H                                      |
| `apps/desktop/src/settings/DesktopClientSettings.diagnostics.test.ts`                                      | R       | B, H                                      |
| `apps/desktop/src/settings/DesktopClientSettings.test.ts`                                                  | R       | B, H                                      |
| `apps/desktop/src/settings/DesktopSavedEnvironments.test.ts`                                               | R       | B, H                                      |
| `apps/desktop/src/updates/updatesTestHarness.ts`                                                           | N       | B, H                                      |
| `apps/desktop/src/window/DesktopWindow.test.ts`                                                            | R       | B, H                                      |
| `apps/desktop/src/wsl/DesktopWslServerTree.test.ts`                                                        | R       | B                                         |
| `apps/mobile/app.config.ts`                                                                                | A       | B                                         |
| `apps/mobile/package.json`                                                                                 | A       | B, 45                                     |
| `apps/mobile/src/features/threads/NewTaskDraftScreen.tsx`                                                  | A       | 45                                        |
| `apps/mobile/src/features/threads/ThreadDetailScreen.tsx`                                                  | A       | 45                                        |
| `apps/mobile/src/features/threads/new-task-flow-provider.tsx`                                              | A       | 45                                        |
| `apps/mobile/src/features/threads/use-composer-command-menu.ts`                                            | A       | 45                                        |
| `apps/mobile/src/features/threads/use-composer-command-menu.test.ts`                                       | A       | 45                                        |
| `apps/mobile/src/state/use-thread-composer-state.ts`                                                       | A       | 45                                        |
| `apps/mobile/src/App.tsx`                                                                                  | R       | B                                         |
| `apps/mobile/src/features/connection/pairing.test.ts`                                                      | R       | B                                         |
| `apps/mobile/src/features/connection/pairing.ts`                                                           | R       | B                                         |
| `apps/mobile/src/features/review/shikiReviewHighlighter.test.ts`                                           | N       | T                                         |
| `apps/mobile/src/widgets/AgentActivity.test.ts`                                                            | R       | B                                         |
| `apps/mobile/src/widgets/AgentActivity.tsx`                                                                | R       | B                                         |
| `apps/server/scripts/migrate-dev-db.test.ts`                                                               | R       | H                                         |
| `apps/server/scripts/migrate-dev-db.ts`                                                                    | A       | H                                         |
| `apps/server/scripts/t3-sqlite-state.test.ts`                                                              | R       | H                                         |
| `apps/server/scripts/t3-sqlite-state.ts`                                                                   | A       | H                                         |
| `apps/server/src/binCli.ts`                                                                                | N       | 35                                        |
| `apps/server/src/cli/app.test.ts`                                                                          | N       | H                                         |
| `apps/server/src/cli/app.ts`                                                                               | N       | H                                         |
| `apps/server/src/cli/config.test.ts`                                                                       | A       | H                                         |
| `apps/server/src/cli/config.ts`                                                                            | R       | H                                         |
| `apps/server/src/cli/pair.test.ts`                                                                         | R       | H, P                                      |
| `apps/server/src/cli/pair.ts`                                                                              | R       | H, P                                      |
| `apps/server/src/cli/theme.test.ts`                                                                        | N       | H                                         |
| `apps/server/src/cli/theme.ts`                                                                             | N       | H                                         |
| `apps/server/src/cli/triage.ts`                                                                            | N       | H                                         |
| `apps/server/src/provider/Drivers/ClaudeDriver.ts`                                                         | N       | 43                                        |
| `apps/server/src/provider/Drivers/ClaudeSkills.test.ts`                                                    | N       | 43                                        |
| `apps/server/src/provider/Drivers/ClaudeSkills.ts`                                                         | N       | 42–44                                     |
| `apps/server/src/provider/Layers/ClaudeCapabilitiesProbe.test.ts`                                          | N       | 43                                        |
| `apps/server/src/provider/Layers/ClaudeProvider.ts`                                                        | N       | 43                                        |
| `apps/server/src/provider/Layers/CodexProvider.ts`                                                         | N       | 43                                        |
| `apps/server/src/provider/Layers/ProviderRegistry.test.ts`                                                 | N       | 42, 43                                    |
| `apps/server/src/provider/Layers/ProviderRegistry.ts`                                                      | N       | 42, 43                                    |
| `apps/server/src/provider/providerMaintenanceRunner.test.ts`                                               | N       | 42                                        |
| `apps/server/src/provider/Services/ProviderRegistry.ts`                                                    | N       | 42                                        |
| `apps/server/src/provider/testUtils/providerRegistryMock.ts`                                               | N       | 42                                        |
| `apps/server/src/serverSettings.test.ts`                                                                   | N       | 42                                        |
| `apps/server/src/serverSettings.ts`                                                                        | N       | 42                                        |
| `apps/web/src/components/settings/settingsSearch.test.ts`                                                  | N       | 43                                        |
| `apps/web/src/components/settings/settingsSearch.ts`                                                       | N       | 42–44                                     |
| `apps/web/src/components/settings/SettingsSidebarNav.tsx`                                                  | N       | 42                                        |
| `apps/web/src/state/query.ts`                                                                              | N       | 42                                        |
| `docs/internals/providers.md`                                                                              | N       | 45                                        |
| `docs/README.md`                                                                                           | N       | 42, 45                                    |
| `packages/client-runtime/src/state/runtime.test.ts`                                                        | N       | 42                                        |
| `packages/contracts/src/auth.ts`                                                                           | N       | 35                                        |
| `apps/server/src/cloud/bootService.test.ts`                                                                | A       | H                                         |
| `apps/server/src/cloud/bootService.ts`                                                                     | A       | H                                         |
| `apps/server/src/entrypoint.test.ts`                                                                       | N       | T                                         |
| `apps/server/src/environment/ServerEnvironment.test.ts`                                                    | N       | 34                                        |
| `apps/server/src/environment/ServerEnvironment.ts`                                                         | N       | 34                                        |
| `apps/server/src/http.ts`                                                                                  | R       | B                                         |
| `apps/server/src/mcp/McpHttpServer.ts`                                                                     | R       | 2                                         |
| `apps/server/src/mcp/toolkits/worktree/registration.test.ts`                                               | A       | 4, 45                                     |
| `apps/server/src/orchestration-v2/Adapters/ClaudeAdapterV2.test.ts`                                        | A       | 8, Saved-agent mentions                   |
| `apps/server/src/orchestration-v2/Adapters/ClaudeAdapterV2.ts`                                             | N       | Saved-agent mentions                      |
| `apps/server/src/orchestration-v2/Adapters/CodexAdapterV2.test.ts`                                         | A       | 8, 28, G, Saved-agent mentions            |
| `apps/server/src/orchestration-v2/Adapters/CodexAdapterV2.ts`                                              | A       | 28, G, Saved-agent mentions               |
| `apps/server/src/orchestration-v2/EffectOutbox.ts`                                                         | R       | 26                                        |
| `apps/server/src/orchestration-v2/FoundationPersistence.test.ts`                                           | N       | T                                         |
| `apps/server/src/orchestration-v2/ProviderAdapter.ts`                                                      | R       | N                                         |
| `apps/server/src/orchestration-v2/ProviderTurnControlService.ts`                                           | R       | 26                                        |
| `apps/server/src/orchestration-v2/ProviderTurnStartService.test.ts`                                        | A       | 28, N                                     |
| `apps/server/src/orchestration-v2/ProviderTurnStartService.ts`                                             | A       | 28, 30, N                                 |
| `apps/server/src/orchestration-v2/RunExecutionService.test.ts`                                             | A       | N                                         |
| `apps/server/src/orchestration-v2/RunFinalizationService.ts`                                               | R       | 30                                        |
| `apps/server/src/orchestration-v2/ThreadLaunchService.test.ts`                                             | R       | 10, 11                                    |
| `apps/server/src/orchestration-v2/ThreadLaunchService.ts`                                                  | R       | 10, 11                                    |
| `apps/server/src/orchestration-v2/runtimeLayer.test.ts`                                                    | R       | 3                                         |
| `apps/server/src/orchestration-v2/runtimeLayer.ts`                                                         | R       | 5, 21, 26, 30, 38                         |
| `apps/server/src/orchestration-v2/testkit/OrchestratorScenario.ts`                                         | N       | T                                         |
| `apps/server/src/orchestration-v2/testkit/ProviderReplayGate.testkit.test.ts`                              | N       | T                                         |
| `apps/server/src/orchestration-v2/testkit/ProviderReplayGate.testkit.ts`                                   | N       | T                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/delegated_task_status/codex_transcript.ndjson`          | N       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/message_steering/codex_transcript.ndjson`               | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/multi_turn/codex_transcript.ndjson`                     | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/plan_questions/codex_transcript.ndjson`                 | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/proposed_plan/codex_transcript.ndjson`                  | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/provider_thread_resume/codex_transcript.ndjson`         | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/queued_turn/codex_transcript.ndjson`                    | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/simple/codex_transcript.ndjson`                         | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/subagent/codex_transcript.ndjson`                       | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/subagent_continue/codex_transcript.ndjson`              | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/subagent_v2/codex_transcript.ndjson`                    | R       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/subagent_v2_nested/codex_transcript.ndjson`             | R       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/thread_fork_native/codex_transcript.ndjson`             | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/thread_fork_native_continue/codex_transcript.ndjson`    | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/thread_fork_native_prior_turn/codex_transcript.ndjson`  | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/thread_fork_native_siblings/codex_transcript.ndjson`    | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/thread_merge_back_continue/codex_transcript.ndjson`     | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/thread_merge_back_siblings/codex_transcript.ndjson`     | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/thread_rollback/codex_transcript.ndjson`                | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/todo_list/codex_transcript.ndjson`                      | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/tool_call_read_only_on_request/codex_transcript.ndjson` | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/tool_call_restricted_granular/codex_transcript.ndjson`  | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/tool_call_workspace_never/codex_transcript.ndjson`      | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/turn_interrupt/codex_transcript.ndjson`                 | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/turn_interrupt_mid_tool/codex_transcript.ndjson`        | A       | G                                         |
| `apps/server/src/orchestration-v2/testkit/fixtures/web_search/codex_transcript.ndjson`                     | A       | G                                         |
| `apps/server/src/os-jank.ts`                                                                               | R       | H                                         |
| `apps/server/src/persistence/Layers/Sqlite.ts`                                                             | A       | 1, 31                                     |
| `apps/server/src/provider/AntigravityInstallation.test.ts`                                                 | N       | T                                         |
| `apps/server/src/provider/Layers/CodexSessionRuntime.test.ts`                                              | A       | G                                         |
| `apps/server/src/provider/Layers/EventNdjsonLogger.test.ts`                                                | N       | 36                                        |
| `apps/server/src/provider/Layers/EventNdjsonLogger.ts`                                                     | N       | 36                                        |
| `apps/server/src/provider/Layers/CodexSessionRuntime.ts`                                                   | N       | G                                         |
| `apps/server/src/provider/T3OrchestrationInstructions.test.ts`                                             | R       | 8                                         |
| `apps/server/src/provider/T3OrchestrationInstructions.ts`                                                  | R       | 8, 45                                     |
| `apps/server/src/provider/testFixtures/codexCollabMockPeer.mjs`                                            | R       | G                                         |
| `apps/server/src/provider/testFixtures/codexMultiAgentWire.json`                                           | R       | G                                         |
| `apps/server/src/scheduledTasks/ScheduledTaskService.ts`                                                   | R       | 12                                        |
| `apps/server/src/server.ts`                                                                                | A       | 5, 9, MCP idle connection patch           |
| `apps/server/src/serviceLauncher.ts`                                                                       | R       | H                                         |
| `apps/server/src/telemetry/Identify.ts`                                                                    | R       | H                                         |
| `apps/server/src/textGeneration/CodexTextGeneration.test.ts`                                               | N       | T                                         |
| `apps/server/src/ws.ts`                                                                                    | A       | 10, 11, 37, 42, 44, 45                    |
| `apps/web/index.html`                                                                                      | R       | B                                         |
| `apps/web/src/branding.test.ts`                                                                            | R       | B                                         |
| `apps/web/src/branding.ts`                                                                                 | R       | B                                         |
| `apps/web/src/commandPaletteBus.ts`                                                                        | R       | 13, 42                                    |
| `apps/web/src/components/ChatView.logic.test.ts`                                                           | R       | 9, 19                                     |
| `apps/web/src/components/ChatView.logic.ts`                                                                | A       | 9, 19                                     |
| `packages/contracts/src/index.ts`                                                                          | N       | 42, 44                                    |
| `packages/contracts/src/rpc.ts`                                                                            | A       | 42, 44, 45                                |
| `packages/client-runtime/src/rpc/client.ts`                                                                | A       | 45                                        |
| `apps/web/src/composer-logic.ts`                                                                           | A       | 45                                        |
| `apps/server/src/auth/RpcAuthorization.ts`                                                                 | A       | 42, 44, 45                                |
| `apps/web/src/components/ChatView.tsx`                                                                     | A       | 9–11, 19, 24, 34, 45                      |
| `apps/web/src/components/CommandPalette.logic.test.ts`                                                     | R       | 15b, 34                                   |
| `apps/web/src/components/CommandPalette.logic.ts`                                                          | R       | 15b, 34                                   |
| `apps/web/src/components/CommandPalette.tsx`                                                               | A       | 13, 15b, 19, 20, 34, 42                   |
| `apps/web/src/components/CommandPaletteResults.tsx`                                                        | R       | 17                                        |
| `apps/web/src/components/ConfirmDialogHost.tsx`                                                            | R       | 22                                        |
| `apps/web/src/components/LegacySidebar.tsx`                                                                | R       | 21, 22                                    |
| `apps/web/src/components/Sidebar.logic.test.ts`                                                            | R       | 16, 19, 21                                |
| `apps/web/src/components/Sidebar.logic.ts`                                                                 | R       | 16, 19, 21, 34                            |
| `apps/web/src/components/Sidebar.tsx`                                                                      | A       | 9, 16, 19, 21–23, 34                      |
| `apps/web/src/components/chat/ChatComposer.tsx`                                                            | A       | 9, Saved-agent mentions, Role library, 45 |
| `apps/web/src/components/chat/ChatHeader.tsx`                                                              | A       | 19                                        |
| `apps/web/src/components/chat/DraftHeroHeadline.tsx`                                                       | A       | 9                                         |
| `apps/web/src/components/chat/MessagesTimeline.test.tsx`                                                   | N       | 7, 14, 24                                 |
| `apps/web/src/components/chat/timelineMinimapItems.ts`                                                     | A       | 24                                        |
| `apps/web/src/components/chat/MessagesTimeline.tsx`                                                        | A       | B, 7, 14, 24                              |
| `apps/web/src/components/chat/QueuedRunsControl.tsx`                                                       | A       | 24                                        |
| `apps/web/src/components/pullRequest/PullRequestListEmptyState.test.tsx`                                   | R       | 19                                        |
| `apps/web/src/components/pullRequest/PullRequestListEmptyState.tsx`                                        | R       | 19                                        |
| `apps/web/src/components/sidebar/SidebarChrome.tsx`                                                        | A       | 6, B                                      |
| `apps/web/src/confirmDialog.test.ts`                                                                       | R       | 22                                        |
| `apps/web/src/confirmDialog.ts`                                                                            | R       | 22                                        |
| `apps/web/src/hooks/useThreadActionMenu.ts`                                                                | A       | 19 — deleted orphan hook                  |
| `apps/web/src/routeTree.gen.ts`                                                                            | R       | generated route registrations for 6/9     |
| `apps/web/src/components/onboarding/WelcomeWizard.tsx`                                                     | R       | 39, B                                     |
| `apps/web/src/routes/welcome.tsx`                                                                          | R       | 39                                        |
| `apps/web/src/routes/_chat.index.tsx`                                                                      | R       | 9, 19, 34                                 |
| `apps/web/src/routes/_chat.tsx`                                                                            | R       | 19, 34                                    |
| `apps/web/tsconfig.json`                                                                                   | R       | B                                         |
| `packages/client-runtime/package.json`                                                                     | R       | 34, 45                                    |
| `packages/client-runtime/src/operations/commands.test.ts`                                                  | R       | 10, 11                                    |
| `packages/client-runtime/src/operations/commands.ts`                                                       | R       | 10, 11                                    |
| `packages/contracts/package.json`                                                                          | N       | 34                                        |
| `packages/contracts/src/environment.ts`                                                                    | N       | 34                                        |
| `packages/contracts/src/model.ts`                                                                          | A       | 32                                        |
| `packages/contracts/src/orchestrationV2.ts`                                                                | R       | 10, 11                                    |
| `packages/contracts/src/server.test.ts`                                                                    | N       | 43                                        |
| `packages/contracts/src/server.ts`                                                                         | N       | 42, 43                                    |
| `packages/contracts/src/settings.test.ts`                                                                  | N       | 42                                        |
| `packages/contracts/src/settings.ts`                                                                       | N       | 42                                        |
| `packages/effect-codex-app-server/scripts/generate.ts`                                                     | A       | G                                         |
| `packages/effect-codex-app-server/src/_generated/meta.gen.ts`                                              | R       | G                                         |
| `packages/effect-codex-app-server/src/_generated/namespaces.gen.ts`                                        | R       | G                                         |
| `packages/effect-codex-app-server/src/_generated/schema.gen.ts`                                            | A       | G                                         |
| `packages/effect-codex-app-server/src/client.test.ts`                                                      | R       | G                                         |
| `packages/effect-codex-app-server/src/protocol.test.ts`                                                    | A       | G                                         |
| `packages/effect-codex-app-server/src/schema.test.ts`                                                      | N       | G                                         |
| `packages/effect-codex-app-server/test/fixtures/codex-app-server-mock-peer.ts`                             | R       | G                                         |
| `packages/shared/package.json`                                                                             | N       | 43                                        |
| `packages/shared/src/devHome.test.ts`                                                                      | R       | H                                         |
| `packages/shared/src/devHome.ts`                                                                           | R       | H                                         |
| `packages/shared/src/model.test.ts`                                                                        | A       | 32                                        |
| `packages/ssh/src/runnerProcess.test.ts`                                                                   | N       | B, H                                      |
| `packages/ssh/src/tunnel.test.ts`                                                                          | R       | H                                         |
| `packages/ssh/src/tunnel.ts`                                                                               | R       | H                                         |
| `pnpm-lock.yaml`                                                                                           | N       | 45                                        |
| `scripts/build-desktop-artifact.test.ts`                                                                   | A       | B, S                                      |
| `scripts/build-desktop-artifact.ts`                                                                        | R       | B, S                                      |
| `scripts/dev-runner.test.ts`                                                                               | R       | H                                         |
| `scripts/dev-runner.ts`                                                                                    | R       | H                                         |
| `vite.config.ts`                                                                                           | R       | H                                         |

The 2026-09-08 import correction adds bounded file-content schemas and shared selection preparation in `packages/client-runtime/src/state/agentPersonas.ts`, plus import/removal commands in the existing orchestration atom module. Clients send selected JSON contents to the chosen environment, never local paths as server destinations. J5 library code validates and atomically persists imports under a process-wide mutation permit, overlays them on source definitions, and preserves launch snapshots on replacement/removal. The Settings registrations remain thin mounts. Focused tests cover nested file selections, single-file selection, atomic rejection, conflicts, persisted imports, concurrent sessions, restoration, snapshots, and RPC scopes. Native picker/browser interaction still requires an authorized client pass.

The imported-agent lifecycle follow-up adds `setImportedAgentPersonaEnabled` under orchestration-operate authorization. J5-owned library storage preserves an optional enabled flag (legacy records default on), strips it from portable definitions/snapshots, and preserves it on replacement. Catalogs expose a disabled availability reason; launch preparation rejects disabled IDs before creating a task. Shared presentation and J5 web/mobile Settings show On/Off beside Remove import. Tests cover persistence, re-enable, launch rejection/receipt replay, replacement, and removal without affecting saved tasks.

The source-agent removal correction adds `removeSourceAgentPersona` under orchestration-operate authorization and exposes Remove from library for folder-loaded agents/examples in the J5 Settings screens. J5 library code atomically persists per-environment source-ID exclusions without deleting user files or snapshots; imported copies can restore excluded IDs. Regression tests cover My Local Scout, restart persistence, unchanged source files/snapshots, rejected new launches, reimport, concurrent removals, and stale source actions after import. The 2026-09-10 restore follow-up (Tyler Barton's #85 review) adds `restoreSourceAgentPersona` to the same J5 RPC group; the catalog lists excluded source and bundled definitions as `removed` entries and both J5 settings screens show a Restore action in place of the trash, so removing a bundled example is no longer a one-way door now that imports are YAML-only. The same day's personal-agent follow-up adds `createAgentPersona` to the J5 RPC group with J5-owned create dialogs on web and mobile (`AgentCreateDialog.tsx`, `AgentCreateModal.tsx`) sharing route and policy fields with the editors; created agents are ordinary imported definitions, so no upstream file changes. A further pass adds `readAgentPersona` (read scope) for instruction editing, Duplicate, and Export YAML on both clients, and makes `acceptedInput`, `inputArtifacts`, and `outputArtifact` optional in the J5 definition schema so personal agents carry no synthetic handoff fields.

The agent-row cleanup places availability badges beside names and keeps switches plus accessible destructive trash icons in the action area on web/desktop/mobile. The current UI uses `removeAgentPersona` (orchestration-operate): J5 library code excludes the source ID before deleting an imported copy, so removed overrides never reveal another definition. Existing import-only/source-only RPCs are retained for compatibility. Source files and snapshots remain intact; reimport restores an agent. Focused tests cover source, import, disabled override, restart, snapshot preservation, and explicit restoration.

The imported-agent editor removes input/output/policy/route metadata from library rows and mounts J5-owned `AgentEditorDialog.tsx` (web/desktop) and `AgentEditorModal.tsx` (mobile). Shared presentation supplies edit drafts and model options from the selected environment. Additive catalog metadata carries editable targets and a content digest, not instructions. The authenticated `editImportedAgentPersona` mutation validates the digest and full definition, increments version, preserves enabled state and unedited fields, and atomically saves only the imported copy. Instructions/source files/snapshots remain untouched. Source entries require importing a copy before editing. Focused tests cover edit persistence, source/snapshot preservation, invalid/stale/removed saves, and editing visibility/model choices.

The import-control cleanup removes the Replace existing IDs checkbox/switch on web/desktop/mobile. Current clients always submit `replaceExisting: false`; conflict errors direct users to edit, remove before reimporting, or choose a different ID. The existing RPC replacement capability remains compatible. This preserves duplicate-ID protection and prevents imports from silently overwriting in-app edits.

The import-confirmation follow-up adds a conflict modal through the existing web/desktop confirmation host and native mobile alerts. Shared client logic retries the same selection only after approval. A typed RPC conflict error supplies names, IDs, and digests; the server checks confirmed digests under the mutation permit so intervening edits or newly conflicting IDs require fresh confirmation. Cancel writes nothing; Replace overwrites local edits, preserves enabled state and saved tasks, and leaves source files untouched. Focused tests cover cancellation, batch replacement, state/snapshot preservation, and stale confirmation.

Selective import replacement adds per-agent toggles to the web/desktop confirmation and a scrollable native mobile modal. Import selected submits approved conflict digests and bounded skipped IDs; the library omits skipped IDs before conflict checks and persistence. Shared retry logic retains skips across fresh confirmations. New agents still import when every replacement is toggled off; an entirely skipped selection writes nothing. Tests cover mixed selections, cancellation, retained skips, and removal during confirmation.

The import entry point is one Import menu with Agent file and Folder options. Web/desktop reuse the existing menu components, while mobile uses the shared native/themed menu wrapper. Both choices retain the existing file/folder pickers, remote environment targeting, and selective replacement confirmation.

Agent imports and configured source folders accept YAML only (`.yaml`/`.yml`); JSON definition files are ignored in folders and rejected on import (2026-09-09 decision). The server parser enforces YAML 1.2 with duplicate-key and single-document checks; aliases and custom tags are rejected. Shared extension filtering, web file acceptance, and native folder traversal all use the one predicate in `packages/contracts/src/j5/agentPersona.ts`. Native single-file selection permits all MIME types and validates extensions afterward because document providers classify YAML inconsistently. The internal import store, configuration, launch snapshots, and the compiled-in example definitions remain JSON. Focused tests cover multiline instructions, YAML extension variants, JSON rejection, and conflicts.

The 2026-09-10 observability and portability pass adds three methods to the same J5 RPC group with no further upstream touches: `getAgentPersonaUsage` (read) aggregates saved-agent history from the upstream projection tables through `json_extract` on `payload_json` in `apps/server/src/j5/agents/agentPersonaUsage.ts`, capturing the session's `SqlClient` inside `makeAgentPersonaRpcHandlers`; `getAgentPersonaLibrarySources` (read) and `setAgentPersonaLibraryFolders` (operate) inventory and rewrite `agent-personas.json`, with read-only `git status` nudges in `agentPersonaLibraryGit.ts` behind an optional `ChildProcessSpawner`. Catalog entries gain an optional `origin`. Copy to environment is client-only (read YAML from one environment, import into another through the existing conflict flow) in the J5 settings screens; the web open-in-editor action reuses the upstream `useOpenInPreferredEditor` hook without modifying it. Never add persona usage columns, tables, or migrations upstream; the aggregation must keep reading the existing projections.

Agent action feedback no longer occupies the Import agents section. Web/desktop use the existing toast manager; mobile renders a dismissible, automatically expiring toast over the library screen. Import/removal results and action errors use toasts, successful toggles remain silent, and pending operations retain disabled controls without adding status text to the import section.

The agent library places the Import menu beside the Scoped agents heading and removes the separate import section and description. Web/desktop use the existing section header action; the mobile library screen renders its own wrapping header row, leaving the shared `SettingsSection` untouched. Import processing, replacement choices, and toast feedback are unchanged.
